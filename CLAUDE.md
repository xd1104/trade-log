# trade-log — 微台指交易日誌 ＋ 早盤儀表板

Benson 每天早上 **08:45~09:30 之間下一單微型臺指期貨（TMF）**，固定 ±100 點停利停損。
這個 repo 有兩個東西：

| | 位置 | 用途 |
|---|---|---|
| **交易日誌 PWA** | repo 根目錄（`index.html` / `js/` / `css/`） | 手機用，記錄真實交易與勝率統計。掛 GitHub Pages |
| **早盤儀表板** | `tools/shioaji/live_panel.py` | 電腦用，即時報價、K 線圖、模擬練習、回顧 |

---

## ⛔ 最重要的一件事：這個時段沒有統計優勢

2026-08-11 用 506 個交易日做過完整驗證（走查驗證 walk-forward、多種模型、多種目標）：

| 測的東西 | 結果 |
|---|---|
| 首根 5 分 K 順勢做 ±100 | 勝率 50.8%、每筆 -4.6 點 |
| 趨勢指數（kNN 相似情境） | 用他真正的規則（±100 觸價）重測，**AUC 0.46~0.49，等於丟銅板** |
| 邏輯迴歸 / 梯度提升樹 | 同樣 AUC < 0.5，**不是方法太弱，是資料裡沒有這個資訊** |
| 每天固定做多（完全不看模型） | 每筆 +1.78 點 —— **比所有模型版本都好** |

**power 天花板**：單筆報酬標準差 89.7 點，506 天只測得出「大於 8 點/筆」的優勢。
任何小到可能是真的優勢，都小到這份資料證不出來。

**所以：UI 上不得出現任何預測、勝率預估、期望值、買賣建議、訊號強度。**
原本的「趨勢指數 0~100」已於 2026-08-12 移除 —— 一個看起來可信卻無效的數字比明顯沒用的更危險。

唯一還沒被否定的假設是**「Benson 自己的判斷有沒有價值」**，所以工具的重心是
「幫他練習、記錄、檢討自己」，不是找訊號。要驗證那個假設約需 100+ 筆他自己的交易。

---

## 資料與商品

- **一律用微台 TMF**，不要跟大台 TXF 混用。
  同一時刻 TXF 今日量 25,045 口、TMF 167,521 口，差 6 倍以上 ——
  混用會讓量能特徵完全失真（踩過）。兩者走勢統計上等價（1 分鐘變動中位數都是 22.0 點）。
- 歷史 1 分 K：`tools/shioaji/tmf_1min.csv`（2024-07 起，排程每天 14:10 累積）
- **即時報價要訂「當月合約」不是 TMFR1**。TMFR1 是給歷史資料用的合成代碼，
  即時訂閱實測 60 秒收到 0 筆。當月合約用成交量自動挑，換月免手動。

## ⚠️ 永豐 API 的坑

- **即時 tick 要在「1 分 K 這一層」覆蓋，不能等合成完 N 分 K 再補**（2026-08-26 修）。
  永豐的 kbars 會給一根**還沒收完的當前分鐘**，所以「kbars 涵蓋到哪一分鐘」永遠含
  當前這分鐘。舊的 `merge_live_tail()` 在合成之後才補，那一分鐘被判定成「已經有了」
  而跳過 ⇒ 畫面一直吃永豐幾秒前的半成品：**價格在跳、K 棒不動，實測差到 19 點**。
  現在是 `overlay_live()`：同一層才能「換掉」而不是「加上去」，量不會重複計。
  只換 kbars 最後那一分鐘（含）之後的部分 —— 前面已經收完的一律以永豐為準，
  面板自己累的量會因為斷線重連而少算，不要拿它蓋歷史。
- **今天的 kbars 要短快取**。`_SESS_OWN` 不快取「今天」，於是前端每 3 秒要一次圖
  就跟永豐打一次 kbars，`/api/bars` 整支 0.5~0.85 秒。今天的 kbars 一分鐘才變一次，
  壓成 20 秒（`TODAY_TTL`）之後是 0.04~0.07 秒，前端輪詢也才敢從 3 秒縮到 1 秒。
- **1 分 K 用「結束時間」標記**：日盤 08:45 開盤，第一根卻標 08:46（涵蓋 08:45~08:46）。
  合成 N 分 K **必須先把時間往回挪一分鐘**還原成起始時間，否則整串偏一根
  （Benson 自己看出來「你的比大戶投快」）。見 `to_timeframe()`。
  ⚠️⚠️ **`one_min_bars()` 的最後一根是 `13:43`，收盤前那一分鐘是缺的**
  （2026-09-07 lab-qa 查證，⛔ 不是【程式下單】那一輪造成的，是既有行為）：
  篩選條件是 `ts.time() < DAY_END`（13:45），而 `ts` 是**結束時間** ⇒ 最大的 `ts` 是
  13:44（涵蓋 13:43~13:44）⇒ 往回挪之後標籤 `13:43`。**13:44~13:45 那一分鐘整根不見**。
  影響：【程式下單】的 `eod`（「13:45 收盤平」）實際拿到的是 **13:44 的收盤價**，
  少一分鐘；那一分鐘裡才觸到 ±100 的日子會被算成 `eod`。
  影響面小（505 天裡 `eod` 只有 40 天）但**是真的**，⛔ 不要在畫面上寫「13:45 的價」。
  要修的話是把條件改成 `<= DAY_END`（並確認回顧分頁與 `session_frame()` 不會多出一根），
  **這一輪刻意不動**（會動到既有三個分頁的圖與 σ 基準，PM 決定另開一輪）。
- **區間端點碰到非交易日會整段回 404**（不是沒資料，是端點無效）。抓歷史要以「週一～週五」為單位，
  整週失敗再退回逐日抓、跳過休市日。
- **本機 csv 的「最後一天」幾乎永遠是半天**（排程 14:10 跑，當晚夜盤還沒發生）。
  所以 `_raw_days()` 不可以把「在本機檔範圍內」直接當成「已完整」——
  `d >= 本機最後一天` 時一律**同時**再跟永豐要一次、與本機資料合併（`drop_duplicates('ts')`），
  否則最新那個交易日的夜盤永遠拿不到、`session_frame()` 永遠拼不出完整交易日。
  只有 `d < 最後一天` 而本機沒有，才是休市日、可以跳過。
- **`_SESS_BACK` / `_SESS_OWN` 的快取鍵要帶 csv 的 mtime**。某天在它還是「本機最後一天」
  的時候被快取起來就是半天，之後排程補了夜盤也不會失效（跟 `day_index()` 同一招）。
- **「因為還沒連上永豐而不完整」的結果不可以當成成功快取**（2026-08-25 修）。
  面板啟動的頭幾秒還沒連線，`_raw_days()` 只拿得到本機 csv ——
  而 csv 的最後一天永遠沒有夜盤（排程 14:10 跑，那時夜盤還沒發生）。
  那份半成品被存進 `_SESS_BACK` 之後，因為快取鍵只帶 csv 的 mtime、
  csv 要隔天 14:10 才會再變 ⇒ **整天的圖都少一段夜盤**，`session_frame()` 的夜盤基準
  往回跳到更早的日子（實測：週二的圖接的是上週五的夜盤，週一整天不見）。
  重開面板也救不了，因為重開又是同一個時序。
  `_raw_days()` 用 `report` 回報「想跟永豐要卻要不到」，`_cached_raw()` 據此標成
  `partial`，隔 `SESS_RETRY` 秒重試；完整拿到之後才永久留著。
- **週六也有 K 線**（週五夜盤延到週六凌晨 05:00）。判定「上一個交易日」不能只看有沒有 K 棒，
  要看有沒有 **08:45~13:45 的日盤 K 棒**，否則昨收會抓到夜盤收盤（差 700 多點）。
- `api.Contracts.Indexs` 只能用屬性存取（`.TSE`），用 `['TSE']` 會 KeyError；合約清單非同步下載要等。
- 加權指數 IX0001 的歷史 1 分 K **只有 54 個破碎的交易日**，無法用來測基差。
- **API Key 綁 IP**：換 VPN／換網路會登入被拒。
- `set_on_tick_fop_v1_callback` 掛在 `api` 上，不是 `api.quote`。

## 交易日 ＝ 前一晚夜盤 ＋ 當天日盤

即時分頁的 K 線圖畫的是**完整交易日**：前一個交易日 15:00 的夜盤 → 當天 13:45 收盤。
Benson 早上下單前要看得到昨晚怎麼走（2026-08-21 提的），只畫 08:45~13:45 等於少一半。

- **週一的「昨晚」是上週五**（夜盤延到週六凌晨 05:00，週日沒有夜盤）。
  夜盤開盤日不能用「減一天」，要往回找最近一個真的有 15:00 以後 K 棒的日子 → `session_frame()`。
- **「今天」是滾動的**：選過去的日期＝嚴格到 13:45 收盤；「今天（即時）」會一路接到今晚的夜盤，
  這樣晚上看盤線不會斷在半路。
- **5 分 K 對得起來**：15:00 到隔天 08:45 是 1065 分鐘（5 的倍數），所以夜盤與日盤落在同一套格線上。
  `to_timeframe()` 要傳 base＝夜盤開盤那一刻，不能沿用「當天 08:45」。
- **時間標籤不再單調遞增**（…23:55, 00:00…, 08:45…）。任何「照時間字串掃陣列」的程式都會在午夜卡住 ——
  前端 `idxAll()` 因此改成只在「該交易日的日盤」那一段找（練習交易一定落在那裡）。
  每根 K 棒都多帶一個 `d`（日期），日期分隔線與夜盤底色都靠它。
- **漲跌基準改用上一個交易日的日盤收盤**（`prev_day_close`），不是圖上第一根的開盤 ——
  含夜盤之後第一根是昨晚 15:00，拿它當基準會跟大戶投對不起來。
- **回顧分頁維持只有日盤**。回顧與 Bar Replay 談的是他 08:45~09:30 的那一單，
  塞進幾百根夜盤只會讓逐根重播沒法用。`/api/bars` 靠 `full=1` 區分兩者。

### 換日：翻頁列 ＋ 迷你月曆

圖上常駐 `◀ 08-20 夜 → 08-21（五） ▶ 今天`，鍵盤 ← → 換日、Home 回即時；
點中間的日期展開迷你月曆（`day_index()` 提供每天的漲跌、震幅、練習結果）。
2026-08-21 從三案（翻頁列／月曆／清單）中選定，清單案捨棄 —— 那些資訊回顧分頁已經有了。

- **休市日要選不到**。以前是一排日期按鈕，國定假日選下去是一張空白圖。
  判定方式：本機 csv 涵蓋範圍內、那個平日卻沒有日盤 K 棒 → 休市。
  csv 最後一天「之後」的平日不算休市（排程 14:10 才併檔，今天與昨天常常還沒進去）。
- **月曆只排週一到週五**，週末沒有日盤。
- **換日到新資料回來之間有一秒的空窗**，那一秒 `barsCache` 還是上一天的。
  標籤要顯示「載入中」並把練習筆數藏起來，否則會把昨天的練習掛在今天的日期底下。
- **換日請求要帶流水號，只認最後一次的回應**（`barsSeq`）。連按 ◀ 時先送的請求可能後回來
  （每換一天伺服器就要重篩 54 萬列），舊那天的資料會蓋回 `barsCache`；
  而且 `barsCache.date` 跟 `curDay()` 剛好又對得上 ⇒ 上面那個「載入中」守衛判定不出來，
  畫面就在「即時」底下顯示別天的 K 線與練習筆數（2026-08-23 實測按 Home 後停在 08-18）。
- **今天不是交易日時，◀ 要落在「最後一個交易日」本身**。`stepTarget()` 找不到 `curDay()`
  時舊寫法退到 `L.length-1` 再減一 ⇒ 週日按 ◀ 從 08-23 直接跳到 08-20，跳過上週五。
- **一根 K 棒都抓不到時，翻頁列與月曆不准跟著圖一起消失**（2026-08-25 修）。
  那個狀態（本機 csv 沒那天、又還沒連上永豐）下「換到有資料的那天」是唯一的自救路徑，
  舊版卻把整張 K 線卡連同翻頁列換成一張只有成交價與動能的小數字卡 ⇒ 使用者被鎖在那一天。
  翻頁列因此抽成獨立的 `pagerHTML()`，K 線卡（`chartSVG` 的 `head`）與沒有圖時的
  `paintFallback()` 各塞一份；月曆一樣包在 `.cheadwrap` 裡（`#cpick` 兩邊共用同一個 id，
  兩張卡互斥、不會同時存在）。`pagerHTML(null)` 表示沒有 K 棒，練習筆數改讀
  日期索引的 `n` / `net`（`/api/bars` 就算一根 K 棒都沒有也照樣附 `days`）。
  fallback 卡的報價與翻頁列要分成兩個節點（`#mfq` / `#mfpager`）——
  成交價每秒在跳，寫成同一串會讓 ◀ ▶ 跟著每秒被重建（跟 `#cupd` 同一個坑）。

### 沒有即時報價時的行為（2026-08-23）

**「有沒有報價」跟「畫不畫圖」是兩件事。** 以前 `Today.price is None` → `status='waiting'`
→ 前端一道門把整個即時分頁擋掉，於是週末、國定假日、13:45~15:00、05:00~08:45
全部是一片空白 —— 連本機明明就有的歷史 K 線與剛做好的日期選單都用不了。

- 後端把「沒報價」分成兩態放進 `STATE`：`quote='closed'`（休市中，正常，不示警）
  與 `quote='nodata'`（應該有盤卻收不到，示警）。判斷用 `market_session()`：
  **只看時鐘＋星期，不含國定假日**（本機沒有假日表），所以假日會被判成 `day` →
  `nodata` 的文案必須同時提到「可能是國定假日」與「可能是連線問題」，不能只喊斷線。
- 前端 `quoteState(s)`：沒有 `quote` 欄位就退回看 `chips.price`（舊狀態／測試治具相容）。
  沒有即時報價時，大字改用**最後一根 K 棒的收盤**，並在 mini 列明寫「非即時」。
- **沒有即時報價一律不准開模擬單**：按鈕 `disabled` ＋ 底下寫原因，後端 `/api/enter`
  再擋一次。這是**紀錄正確性**（拿舊價記進練習成績＝假成績），不是 UX 取捨，不要放寬。
- 休市中的連線燈是中性灰（`.dot.off`），不是紅色 —— 週末沒報價不是故障。

## ⚠️ 面板開發鐵律

- **有 `max-height` 的 flex 直欄，子元素一定要 `flex:none`**（2026-08-25 踩到）。
  預設 `flex-shrink:1` ⇒ 內容超過高度時不是捲動，而是把每一列**壓扁**
  （實測 `.trade` 從 107px 被壓成 21.6px，字整片切掉）。
  **筆數少的時候完全看不出來**（總高沒超過 max-height），所以測清單一定要用
  「超過容器高度的筆數」測，不能只放三筆就過。
- **同特異度的 CSS 後者贏**。`.trade .noteline` 曾經在檔案裡出現兩次，
  後面那條把前面壓成一行的設定整個蓋掉、又量不出所以然。改樣式前先 `grep` 一次選擇器。
- ⛔⛔ **資料不完整時，絕對不可以「猜」夜盤是哪一天**（2026-09-02 他回報「K 線圖一直
  變來變去」，截圖是面板重啟後第 60 秒與第 93 秒：**先畫出前天的夜盤，連上永豐後才換成昨晚**）。
  根因在 `session_frame()`：`n = nights["ts"].dt.date.max()` 取的是
  「**手上這批資料裡**最近一個有夜盤的日子」。剛啟動還沒連上永豐時只讀得到本機 csv，
  而 csv 的最後一天永遠缺夜盤（排程 14:10 跑，那時夜盤還沒發生）⇒ 挑到前天、畫出錯的一晚，
  **而且畫面上完全看不出來**。程式其實知道那份不完整（`_cached_raw` 的 `partial`），
  只是沒拿它擋畫面；`SESS_RETRY=60` 又讓那份錯的撐滿一分鐘。
  現在：`partial` 一路傳到 `/api/bars`，不完整就**只畫當天日盤**（那段是對的）、
  標題寫「夜盤資料載入中」，並用 `SESS_RETRY_PARTIAL=8` 縮短空窗。
  **少一段看得出來，畫錯一天看不出來 —— 他早上是照這張圖決定要不要進場的。**
  守衛：`test_session_frame.py`（離線、不連永豐，**20 項**；變異測試驗過拿掉那道會紅 3 項）。
- ⛔ **價格軸要黏住，不可以每根新 K 棒就重算一次**（2026-09-02 他回報「開盤時 K 線圖一直
  變來變去」）。實測回放當天資料：**08:45~09:30 軸變了 8 次、09:30 之後 0 次**。
  原因不是價格創新高低，是**視窗固定只顯示最後 60 根**：每多一根新的、左邊就掉一根舊的
  ⇒ 昨晚夜盤的最高點被一根一根擠出畫面 ⇒ `hi` 一路縮（實測上緣 46962→46934→46868→46836→46815）
  ⇒ **整張圖所有 K 棒重新定位**。最後那根還是沒收完的即時棒，高低點每個 tick 都在動。
  修法兩層：① **貼齊整數刻度**（`niceStep()`，1/2/2.5/5/10×10ⁿ）—— 差幾點根本動不到軸，
  順便讓刻度從 46423/46501/46579 變成 46400/46500/46600；② **遲滯** —— 舊軸還包得住新資料、
  且資料沒有縮到小於舊軸的 55%，就沿用。修完同一份資料回放：**開盤 8 次 → 1 次**。
  ⚠️ 縮放／平移／換日要能重新貼合，所以 `VIEW.n`／`VIEW.end`／日期都要進遲滯的 key；
  刻度線要從 `lo` 往上取第一個 step 整數倍開始畫（vz/voff 之後 hi/lo 不再是 step 的倍數）。
  ⚠️ 回顧分頁的圖（`#rsvg`）是另一段程式，不共用 `chartSVG()`。
- ⛔ **背景輪詢的節奏要扣掉工作時間**（同一天）。加權指數「更新有點慢」：實測期貨成交價
  0.4 秒跳一次、**指數 6.2 秒才動一次**，但程式寫的是 3 秒 —— 因為 `api.snapshots()`
  本身要約 3 秒，`sleep(3)` 又疊在後面 ⇒ **實際週期 = 工作時間 ＋ 設定值**。
  改成 `sleep(max(0.3, INDEX_EVERY - took))`，並把單次耗時記進 `INDEX["ms"]`、
  把新鮮度 `idx_age` 放進 `chips` —— 下次要判斷「慢在網路還是慢在我們」才有數字可看。
- **圖表容器只建一次，之後只換 `<svg>` 內容**。整塊 innerHTML 重繪會打斷使用者的縮放/拖曳並閃爍。
- **每 0.5 秒重繪的東西一律不准掛動畫**。`#chead` 每次報價變動就整個重建，
  掛上 CSS animation 會變成一直閃。進場動畫用 `body.boot`（開站後 1.1 秒就移除），
  一次性的效果（K 線展開）用「加 class → 計時器移除」，不能讓 `clip-path` 留在元素上
  （留著的話之後每次重繪都被裁，圖會缺一角）。
- **載入骨架的高度要跟真圖分毫不差**。量過：標頭 44、legend 18.8、圖 `aspect-ratio:1040/470`、
  提示 16.5、mini 30.8；骨架沿用同一組 class 再補 `min-height`，實測差 0.1px、CLS = 0。
  骨架裡的假 K 棒要**絕對定位** —— 當一般 flex 子元素的話，它們的百分比高度會反過來
  把 `.cwrap` 撐高（351 → 553），骨架比真圖高一截，換過去照樣跳。
- **任何「只存在記憶體」的狀態都要能從檔案還原** —— 面板會自動重啟（看門狗）。踩過三次：
  - 重啟後 `TODAY_TRADES` 是空的 → 再下一單就覆蓋掉當天稍早的紀錄（**弄丟過真實資料**）→ `load_today_trades()`
  - 重啟後開高低量從那一刻重算 → 震幅顯示 20 點 → `seed_from_bars()`
  - 半夜啟動時 `session["state"]` 是 None → 收到的報價全丟 → 啟動時一定建立狀態
- **跨午夜要清掉「日盤專用」的欄位**（2026-08-25 修）。主迴圈跨日時原本只做
  `session["date"] = today`，Today 物件與 `TODAY_TRADES` 都不動、要等 08:30 才重建。
  於是 **00:00~08:30 這段（正好是他早上開面板的時間）**畫面上的開高低、震幅、
  位階、量能、跳空全是**昨天日盤**的，時鐘卻是今天；練習清單也把昨天那筆算成「今天」，
  凌晨夜盤再下一單還會把昨天那筆一起寫進今天的檔案（重複計入勝率）。
  `reset_for_new_day()` **只清 open/high/low/vol**，`minute_bar` / `minute_close`
  一定要留著 —— 夜盤還在跑，那是 `merge_live_tail()` 補最新那根 K 棒與算動能的來源。
- ⛔⛔ **逐筆落地：`on_tick` 裡一行同步磁碟 I/O 都不准有**（2026-09-07 加）。
  以前每一筆報價收完就丟，只留下 `ticks` 的計數（`morning_logs/2026-08-11-live.json`
  寫著 `"ticks": 42718` —— 那天四萬多筆，留下來的只有那個數字），
  而 `tmf_1min.csv` 最細只到一分鐘 ⇒ **一分鐘內發生什麼事永遠補不回來**。
  現在 08:45~09:30 的每一筆成交與買賣價寫進 `tick_logs/YYYY-MM-DD.jsonl`（已 gitignore）。
  - **`on_tick` 跑在永豐 SDK 的回呼執行緒上，它更新的價就是停損看的那個價**
    （永豐沒有停損單，停損活在 Python 迴圈裡）。一筆卡 5ms、早上四萬筆就是 200 秒，
    **塞住的時候塞住的是他的停損**。所以回呼裡只做 `deque.append`（實測 2.4µs/筆、
    最慢單筆 0.28ms），格式化與寫檔全在 `tick_writer.py` 自己的 daemon 執行緒、每秒一批。
  - **佇列滿了寧可丟資料也不阻塞**，但丟的是**新來的那一筆**（不是 `deque(maxlen=)` 那種
    默默丟最舊的），而且要留痕跡：計數 ＋ 檔案裡寫一列痕跡 ＋ 主控台警告。
    寫檔失敗（磁碟滿）那幾列同樣要算進 `lost_io`。**不可以安靜地少。**
  - **痕跡列（`k=x`）刻意沒有 `t` 欄位**（2026-09-07 QA 指出）。`t` 在這個檔裡的定義是
    「永豐給的交易所時間」，檔頭 `clock` 那句話講得斬釘截鐵；而痕跡列**沒有交易所時間可用**
    （丟掉的那一筆連進佇列都沒進來），第一版拿 `_hms(datetime.now())` 填 `t`
    ＝ **拿本機時鐘冒充交易所時間，而且冒充得看不出來**。現在拆成兩欄：
    `wt`＝本機時鐘（而且是**寫檔執行緒 drain 的時刻**，不是事情發生的時刻，最多差一個
    `flush_every`）、`after`＝同一批裡前一列的交易所時間（缺口的起點下界，缺口終點看下一列）。
  - **檔案不保證時間單調遞增**。照收到的順序寫（重連補送／SDK 亂序／重啟接檔都會讓它往回跳），
    檔頭的 `order` 欄位明講了這件事 —— 分析腳本很容易假設有序。
  - **一批之內日期跳超過 `MAX_DAY_SWITCH`（4）次就停止分檔**（2026-09-07 加）。
    每換一次日期就 close + open + 寫一列檔頭：QA 實測 200 筆交錯日期 ⇒ 一次 drain
    從 1.8ms 變 **97.1ms**，兩個檔各 200 列裡有 **100 列是檔頭**（檔案被檔頭灌爆）。
    超過就全部寫進這一批的第一個日期，並留一列 `{"k":"x","why":"date_thrash"}`。
    全程在寫檔執行緒、`on_tick` 不受影響 —— 但那條執行緒卡住就是「這段沒錄到」。
  - **一定是 `open("a")`**。看門狗重啟是常態，覆寫＝把當天稍早的資料弄丟
    （`TODAY_TRADES` 已經這樣弄丟過一次真實資料）。重啟只會多一列檔頭。
  - **時段判斷用永豐給的 `tick.datetime`，不是本機時鐘** —— 檔案裡存的就是那個時間，
    兩把尺不一致會寫出「時段外的時間戳」；而且分析要跟 1 分 K 對得起來。
  - 量（2026-09-07 QA 磁碟實測修正，**舊版的 46 bytes/列是錯的**）：那個數字是
    「JSON 字串長度、**不含行尾**、而且只算 tick」。實際上 Windows 文字模式寫出去是 **CRLF**
    （每列 +2），而 bidask 那型比 tick 長 6 個字元 ⇒ 上磁碟 **tick 48／bidask 54 位元組**，
    平均 **51.2**（QA 實測）。照 tick 42.7k ＋ bidask 1~2 倍算 ⇒
    **一早上 4.36~6.66 MB、一年約 1.05~1.60 GB**（QA 的算式取 6.56 MB / 1.57 GB，
    差在 bidask 倍數怎麼取，同一個量級）。
    ⚠️ `test_tick_writer.py` ⑤ 有一條**絆線**：位元組數是拿 `tick_writer._line()` 真的產一列
    量出來的，改欄位就會紅 —— 紅了請順手把上面這幾個數字一起更新，不要只改測試。
  - 守衛：`test_tick_writer.py`（58 項，離線；原本 36 項）。
    ⚠️ **不要再寫「突變測試 N 個全紅」**（原本這裡寫「10 個全紅」）：dev 自己做的那 10 個
    **全在 tick_writer.py 那一側**，也就是**已經有測試守著的那一半**。lab-qa 獨立做了 20 個，
    **6 個打不紅**，而且全部落在 `live_panel.py` 的**接線**那半 ——
    `TICKS.start()` 換成 `pass`、建構式的 `win_start`/`win_end` 拿掉或改成 `DAY_END`、
    `st.feed()` 與 `TICKS.tick()` 對調、`fh.flush()` 換成 `pass`、
    `_in_window` 的 `ts is None` 防呆拿掉 —— 每一個都是 36/36 全綠。
    **突變測試的價值在「找出哪裡打不紅」，寫成「全紅」會讓下一個人以為這塊守好了。**
    教訓：**守衛蓋的是 `tick_writer.py`，接線那半是後來才補的** —— 寫測試時很自然只驗
    「模組本身對不對」，忘了驗「有沒有人真的把它接上電」。而看門狗會自動載入磁碟上的任何一版，
    少一行 `TICKS.start()` ＝ **整個早上零落地**，要到 09:30 印成績單才看得出來，
    那時候那天的資料已經沒了。
    現在補上的守衛（每一條都用突變證明過改壞了會紅，11/11 全紅）：
    - ⑥b 接線三條（AST 讀 `live_panel.py`）：`main()` 裡有 `TICKS.start()`／
      窗口 keyword **比名稱不比數值**（`SESSION_OPEN`／`WATCH_END` —— 比數值的話，
      有人把 `WATCH_END` 改掉一樣過）／`on_tick` 裡 `st.feed()` 排在 `TICKS.tick()` 前面。
    - ②b `fh.flush()`：**必須開子行程才驗得到**。上面每一節收尾都 `w.stop()`（＝關檔＝
      緩衝一定落地），所以 `flush()` 在同一個行程裡**結構上不可能承重**；
      而它要防的正好是 stop() 不會被呼叫的情境。做法是子行程寫幾筆 → sleep 讓寫檔執行緒
      跑一輪 → `os._exit(9)`（不呼叫 `stop()`）→ 父行程斷言那幾筆在檔案裡。
    - ①b `_in_window` 的 `ts is None`（熱路徑上唯一的防呆，例外會冒回 SDK 的回呼執行緒）。
    - ③ 加一條「400 筆**全部**寫出去」：原本的兩條自證（`sink.writes >= 1`、
      `sink.slept >= 50ms`）**一列就足以成立** ⇒ QA 把 `tick()` 改成「只放第一筆、
      之後靜靜丟掉」，③ 照樣五項全綠（一筆都沒錄當然快）。
    - ④c 日期亂跳、④ 痕跡列的 `wt`/`after`、⑤ 檔頭的 `order` 與位元組絆線。
  - `tick_recorder.py`（每 0.1 秒讀 `/api/state` 的**取樣**工具）寫的是
    `YYYY-MM-DD-polled.jsonl`，**刻意跟逐筆分開** —— 欄位完全不同，混在同一個檔
    會讓人把取樣當成逐筆。那支保留：它不用重啟面板就能開始錄（盤中他手上可能有部位）。
    ⚠️ 2026-09-07 起【細節】分頁**讀得到它**了（標成「取樣」、量柱停用），見下面
    【細節】分頁那一節。**分檔這件事不變** —— 分開的目的一直是「看得出來是哪一種」。
- ⚠️⚠️ **「不碰 `state_lock`」≠ 零成本：重活跑在 HTTP 執行緒上，會拖慢主迴圈**
  （2026-09-07 lab-qa 用獨立治具實測，寫在這裡是因為文件裡原本一個字都沒有，
  下一個人只會看到「不碰 `state_lock`」而以為零成本）。這支面板是**單行程**，
  `ThreadingHTTPServer` 的工作執行緒跟主迴圈搶的是同一個 GIL。
  **停損檢查週期**（主迴圈 4Hz，平常 276 ms）：
  ・【細節】冷解析一天逐筆（~0.5 秒）→ **970 ms**（新分頁）
  ・`/api/bars` 換日（**既有功能**，連按 ◀ 就會發生）→ **1732 ms**
  ・`/api/review`（既有功能）→ 914 ms
  ⇒ **新分頁比既有的換日還輕**，不是新洞；但「最慢會停在一秒級」是這支面板
  **既有的架構性質**，寫下來才有人記得。同一件事在 `/api/state` 上的形狀是
  **中位 4.4 → 8.0 ms、但冷解析期間 max 758 ms**（只報中位會把尾巴蓋掉 ——
  他回報「面板卡了一下」時要查的是 max，不是中位）。
  **待辦（這一輪刻意不做）**：要治的方向是把 `_tick_load` 分段
  （每 N 列 `time.sleep(0)` 讓出 GIL），代價是解析變慢一點；
  動之前先量一次現況，別憑感覺調。

### 【細節】分頁（逐筆早盤圖）— 2026-09-07 加

第三個分頁「細節」（`data-tab="tick"`，放在**即時與回顧中間**）：08:45~09:30 的**逐秒**圖。
規格 `tools/shioaji/TICK-TAB-SPEC.md`。跟另外兩張圖的區隔**不是「今天 vs 那 45 分鐘」，是多細**
——他那一單 290 秒，在即時的 5 分 K 上是 1 根、30 秒桶約 10 根、1 秒桶 290 根。
預設圖種是**秒 K**（K 棒比折線多給的是「這一段摸到多高多低」，那正是 ±100 觸價在意的）。

- ⛔ **Canvas 不是 SVG，而且像素分桶是必須不是優化。** 2026-09-07 實測（40,000 點、
  headless、真滑鼠拖 60 步）：SVG path 155.1ms/6.4fps、Canvas 全部點 152.8ms/6.5fps、
  **Canvas 像素分桶 15.4ms/65fps**。即時分頁的 `chartSVG()` 那套不可以沿用。
  分桶是**無損的**：一個像素欄只留 min/max/first/last，逐欄比對「分桶 vs 全部點」的
  最高最低必須 100% 相等（`tick-tab.mjs` ③ 在守，`maxColDiff===0`，
  負控組改成「每 30 筆取一筆」會紅）。⛔ 絕不可以用等距抽樣 —— 那會漏掉尖峰。
- ⛔ **後端分桶，一天回 1 秒桶的 columnar 陣列**（實測 6.27MB 的 jsonl → 167KB payload，
  小 38 倍，而且**上限固定**：一天最多 2,700 個桶，不管他成交 8 萬還是 20 萬筆）。
  ⚠️ **「最多 2,700 個桶」是靠後端切窗口守住的，不是自然發生的**（2026-09-07 補）。
  逐筆檔靠 `tick_writer` 的窗口自然落在 08:45~09:30 裡面，**但取樣檔不是** ——
  `tick_recorder.py --until 13:45` 是它自己說明裡就有的用法，那種檔一路錄到下午，
  照收會給出 **8,000+ 個桶**（lab-qa 實測 8,410，其中 7,649 個在 09:30 之後），
  關掉「時間軸固定」之後整個早上被壓成一小段。
  所以 `_tick_load()` / `_tick_load_polled()` **兩條路都切窗口**
  （`_tick_inwin()`，`[TICK_SEC0, TICK_SEC0+TICK_SPAN)`），
  **被切掉幾列一定要講出來**（`outwin` → 副標寫「另有 N 列在 08:45~09:30 之外」）。
  ⛔ 不要只在取樣那條路切 —— 那個不變式是**整頁**的性質（前端固定時間軸與效能都靠它），
  對某一種檔失效等於在逐筆那半埋一顆看不見的地雷（有人動 `WATCH_END` 就會踩到）。
  不做 gzip、不做 delta 編碼（省到 61KB 但兩端各一份編解碼，最容易寫錯又最難察覺）。
  ⛔ **不做 `/api/tick/raw`**：1 秒是原子單位（繪圖區 976px、45 分鐘 ⇒ 一個像素 2.77 秒；
  放大到 1 分鐘時一根 1 秒 K 已經佔 16px）。
- ⛔ **`/api/tick/days` 只准「列目錄 ＋ 8KB 嗅探 ＋ stat」**。一年後 240 個檔 × 5MB，
  全解析就是每次切分頁卡一分多鐘。連帶：**清單裡沒載入過的日子不顯示估算筆數**。
- ⛔ **判斷「這是哪一種檔」不可以只看檔名**：`tick_logs/` 同時住著
  `YYYY-MM-DD.jsonl`（逐筆）與 `YYYY-MM-DD-polled.jsonl`（`tick_recorder.py` 的取樣，
  schema 完全不同）。而且 `tick_recorder.py` 加後綴**之前**留下的檔就叫
  `YYYY-MM-DD.jsonl`、內容卻是輪詢 schema。兩道都要：檔名完全符合正則 ＋ **嗅探內容、
  而且種類要跟檔名一致**（逐筆＝開頭 8KB 至少有一列帶 `k`；取樣＝至少有一列帶 `price`
  ＋讀得懂的 `t`、而且不帶 `k`）。嗅不到就不列進清單、**在 console 印出原因**
  （⛔ 不可以靜靜跳過，也 ⛔ 不准自作主張改名或刪除）。
- **取樣檔（`-polled`）也要看得到，但畫面上要明講那是取樣**（2026-09-07 加）。
  原本只收逐筆檔，於是他 09-07 早上唯一那份行情（面板 12:16 才重啟、逐筆落地那時才生效；
  盤中是用 `tick_recorder.py` 錄的，含他當天那一單）在畫面上**完全不存在**。
  **「不要讓取樣冒充逐筆」的正解是標示清楚，不是整個不給看** —— 這兩件事被搞混了一輪。
  ⛔⛔ **這一輪放寬的是「檔名」，不是 schema 檢查**：一個叫 `YYYY-MM-DD.jsonl`
  卻裝著取樣內容的檔，**必須繼續被擋掉並列進 `skipped`**（那是這條規則的原始目的）。
  - `/api/tick/days` 每天帶 `kind`（`tick`／`polled`）＋ `alt`；`/api/tick/day` 把取樣檔
    轉成**跟逐筆同一種 1 秒桶 columnar**（前端不寫兩套），並帶 `has_vol`／`ms_med`／`nopx`。
  - ⛔⛔ **`tick_day()` 要按「驗證過的種類」挑，不可以按「檔案存不存在」挑**
    （2026-09-07 lab-qa 退件 M1）。`_tick_load()` / `_tick_load_polled()` **自己會先嗅探、
    嗅不過就回 None**，所以 `tick_day()` 那兩行的語意是「先要**合格的**逐筆，沒有才退到取樣」。
    舊寫法只看 `path.exists()` ⇒ 一個「逐筆檔名 ＋ 取樣內容」的檔（`tick_days()` 已經把它
    列進 `skipped`）照樣會被端出來，而且**把同一天真正的取樣檔整份蓋掉**：
    清單寫「取樣」、點進去圖上寫「逐筆」、沒有金籤、成交量那顆變回可以按、
    空狀態寫「這天只有檔頭，一筆成交都沒有錄到」（**一句假話**，那天有 1,091 列）。
    ⚠️ 這個洞在 `939dce2` 就存在，只是那時取樣檔根本不進清單、那一天點不到 ——
    **放寬檔名那一輪把它叫醒了**。通則：**放寬一道入口之前，先看那道入口下游還有誰
    是用另一把尺在挑東西的。**
    守衛：`tick-backend.py` ②c —— 對治具裡**每一天**斷言
    `tick_day(d)["kind"] == tick_days() 清單上那天的 kind`（兩把尺必須是同一把），
    負控組就是 QA 那個突變（把 `tick_day()` 的兩行對調）＋「拿掉 `_tick_load` 的嗅探」。
  - **被跳過的檔要在清單上一直看得到**（不是只在空狀態）。舊版 `skipped` 提示只在
    `days.length===0` 才出現 ⇒ 只要有任何一天有資料，「我明明有錄怎麼看不到」就完全無解。
  - **同一天兩種檔都有 ⇒ 逐筆優先**，另一份標 `alt:true`（清單寫「＋取樣檔」，長說明放 title
    —— 長字串會把 `.meta` 擠到換行、那一列比別列高一截）。
    逐筆是取樣的上位集合（有每一筆、有量），而且是面板自己寫的、不會被外掛工具中途關掉。
  - ⛔⛔ **取樣檔沒有單筆成交量**（`vol_ratio` 是「**累積**量 ÷ 歷史中位」，不是量）⇒
    量柱疊圖 **disabled ＋ 寫出原因**，跟「加權」那顆同一套。守衛是
    「開不開那顆疊圖，畫出來的像素要一模一樣」。**絕不可以湊一根看起來像的量柱。**
  - **買賣價有** ⇒「折線＋價帶」對取樣日照樣能用（實測他那份 100% 的桶都有 bid/ask）。
  - **標籤要三處都在，而且要真的畫在畫面上**：副標（`逐秒` → `取樣 約 X 秒一筆`）、
    圖上左下角一張金色籤、日期清單**每一列**的種類標（⛔ 逐筆也要標 ——
    只標取樣的話，沒有標記等於「不知道」而不是「另一種」）。
    圖上那張籤放**左下角**：左上角被「我那一單在左/右邊」佔走、右側是「±100 在畫面外」
    與「09:30 收手」，三張擠在同一行會互相壓掉字。
  - **缺口門檻不改**（還是 30 秒；實測他那份取樣的相鄰列中位 457ms、最大 1,628ms、
    秒與秒之間最大只跳 2 秒 ⇒ 30 秒還差一個數量級）。**改的是那句話**：
    取樣工具「有變才記」，空白那段有兩種可能 ⇒ 取樣日**不可以**寫「不是沒行情」
    （那是我們證不了的話），改成「這段沒有取樣到（沒錄到，或報價一直沒變）」。
  - **`auto` 桶寬對取樣日的下限是 2 秒**。用他 2026-09-07 那份真取樣檔（2,972 列）實測：
    1 秒桶 1,504 個、平均 **1.98 個點**，其中 **26.7% 高＝低**（401 個 —— 秒 K 退化成
    一根橫線，看起來像「這一秒沒動」）；2 秒桶 755 個、平均 3.94 個點，
    高＝低降到 **0.9%**（7 個）。⛔ 只動 `auto`，他手動按「1 秒」還是給他 1 秒。
    ⚠️ **2026-09-07 更正**：這裡原本寫「1 秒桶**一半**只有 1 個點」，那個數字沒有量過
    （lab-qa 用同一份真檔實測是 **13.3%**＝200/1504）。真正撐住這個決定的是
    **高＝低那 26.7%**。**這個專案的設計備忘裡不可以放沒量過的數字 —— 下一個人會照著它做決定。**
  - **加權那顆的停用理由要分兩種寫**：逐筆檔真的沒有 `idx`，**取樣檔有**
    （`tick_recorder.py` 有記）。寫同一句就一定有一句是假的。這一輪仍然不畫那條線。
  - **`_tick_chunk()`**（快取有效性／檔案變小／讀到半列）**是兩條路共用的** ⇒
    那三道守衛的負控組一個就同時蓋住逐筆與取樣。代價是 `tick-backend.py` 的兩個突變目標
    從 `_tick_load` 搬到 `_tick_chunk`（⑤ 半列、⑤b M5 只比 mtime）。
- ⛔⛔ **「安靜地少」的反面是「大聲地全沒了」，兩個一樣不可接受**（2026-09-07 lab-qa 實測）。
  jsonl 裡只要**一列**的 `price`／`p`／`v`／`n` 不是數字（字串、NaN），
  舊寫法會在 `px > row[1]`（TypeError）或 `round(float(px), 1)`／`int(...)`（ValueError）
  當場炸掉、例外一路冒到 handler ⇒ **那一天整份回 500，1,091 列好資料一列都看不到**。
  所有數值欄位一律過 `_tick_num()`（**bool 要另外擋**：`isinstance(True, int)` 是 True）：
  **一列壞資料只准弄掉那一列並計入 `bad`，不准弄掉一整天。**
  ⚠️ 讀不懂的 `bid`/`ask` 例外：當成「那一列沒有買賣價」、**不計 bad** ——
  價本身讀到了，把整列說成「讀不出來」反而是假話。
  守衛：`tick-backend.py` ②b-2（負控組拿掉型別檢查 ⇒ 真的丟 `ValueError`）＋ ⑨-2c（端點 200）。
- ⛔ **每一列都要有去處**：`n + bad + nopx + outwin ＝ 檔案裡的總列數`。
  這條等式是「有沒有東西被安靜吃掉」唯一的機器判準（`tick-backend.py` ②b-2／②b-4 在斷言）。
- ⛔ **增量合併是「同 `s` 覆蓋、其餘 append」，不是 `concat`**。`from=<秒>` 回的桶
  **含 `from` 本身**（那個桶上次拿到時還沒收完）⇒ concat 會出現兩根同一秒的 K 棒，
  **圖上完全看不出來，只有量會變兩倍**。同一族：讀增量時最後一列若不是 `\n` 結尾要丟掉、
  byte 位移退回上一個換行處（append 不是原子的，一定會讀到半列）。
- ⛔ **canvas 的 `width`/`height` 只有尺寸真的變了才准動**（`tkFit()` 的 early-return）。
  寫 `canvas.width`（就算寫同一個值）會重新配置整張後備緩衝區並清空 ——
  demo 實測 2,212 點時一次 draw 從 2ms 變 **129ms**。它在熱路徑上（每次 draw 都呼叫）。
- **價格軸：`niceStep()` ＋ 遲滯，遲滯 key ＝ `日期|圖種|視窗起|視窗迄|桶寬`。**
  ⚠️ 實測發現**真正在守這張圖的是 `niceStep()` 不是遲滯**：時間軸預設固定 08:45~09:30、
  資料只增不減 ⇒ 新範圍永遠不會被舊軸包住，遲滯的 early-return **結構上進不去**
  （拿掉之後整條軸的變化序列逐字相同）。拿掉 `niceStep` 的貼齊則從 3 次變 16 次。
  遲滯**目前不承重**；而且**以現在的 key（含視窗起訖 `t0`/`t1`），做滑動視窗也不會承重**
  ——`tkFollow()` 每往前一格就改一次 `TK.view.t0/t1` ⇒ **key 每一格都變** ⇒
  滑動視窗正好是遲滯**最沒用**的場景。真要做滑動視窗，**得先改 key**
  （例如只帶「視窗長度」不帶起訖），不是留著等有人來用。
  ⚠️ 舊版這裡寫「遲滯留著是為了……將來有人做滑動視窗」，**方向是反的**
  （2026-09-07 lab-qa 查證後更正）。**寫「遲滯在守」同樣是錯的，別再照抄那句話**。
- ⛔ **秒級時間軸一律印完整 `HH:MM:SS`**（只印 MM:SS 時「14:05」會被讀成下午兩點）。
- ⛔ **±100 與交易標記都不准撐大價格軸**。那 45 分鐘的真實振幅常常只有 30~60 點，
  硬把 ±200 塞進來會把唯一要看的波動壓成一條直線。畫不到就掛邊緣籤。
  ⚠️ 真實單的 `exit` 可能是 `null`，`Math.min(lo, entry, exit)` 會把它當 **0** ⇒
  價格軸整個掉到 0（2026-09-02 踩過）。餵進算式前一律先過濾。
  ⚠️ 邊緣籤有三張會擠在同一行（「我那一單在左/右邊」上緣、「±100 在畫面外」、
  「09:30 收手」下緣）—— y 要錯開，第一版就把彼此的字壓掉一半。
- **缺的那段畫斜線，而且那句話一定要說出「不是沒行情」**。空白的那一段長得跟
  「行情沒動」一模一樣，他早上是照圖判斷的。⚠️「我們漏了」（沒有錄到）與
  「時間還沒走到」（還沒到，只有今天有）的文案**必須不同**。
- **加權指數這顆疊圖是 disabled 的**：**逐筆檔**只有成交與買賣價，**沒有指數**。
  規格 §5.2 寫了要畫，但資料源根本沒有那一欄 ⇒ 停用並寫出原因，
  ⛔ 不可以拿別的來源硬湊一條「看起來像」的線。（同理 §5.3 已明訂不做基差。）
  ⚠️ 2026-09-07 更正：**取樣檔（`-polled`）其實有 `idx`**（`tick_recorder.py` 有記），
  所以那顆的 title **不可以兩種檔寫同一句**（會有一句是假的）；取樣日寫
  「取樣檔裡有加權指數，但這一頁沒有做這條線」。**要不要真的做那條線是 PM 的決定，還沒做。**
- **翻頁列沿用即時分頁那一套（`◀ 日期 ▶ 今天`、鍵盤 ← → Home），但把月曆換成清單** ——
  這裡的母體是「有逐筆檔的日子」，上線第一週只有 0~3 天，一個 95% 格子都是灰的月曆
  傳達的是「壞了」而不是「沒有資料」。清單每一列寫筆數與缺口（全站沒有第二個地方看得到）。
  ◀▶ 走的是**清單索引**不是「日期減一天」；日期鈕寬度不准隨狀態變（載入中只轉灰）；
  「今天」鈕在今天沒錄到時是 **disabled 不是消失**（消失＝整條靠右對齊的 r1 位移）。
- **`TK.date`（想看哪天）與 `TK.data.date`（手上這份是哪天）是兩個欄位**，不相等就畫骨架、
  副標寫「載入中…」、筆數與缺口整組藏起來。⚠️ 這一道跟流水號守衛是**互相備援**的：
  探針要兩道都拿掉才紅得起來（只拿掉一道會誤以為「這道沒有用」）。
  流水號守衛真正在守的是**同一天的兩個請求誰後回來**（先送的後回來會用比較舊那份蓋掉新的）。
- ⛔ **這一頁一行都不碰 `on_tick`／佇列／`state_lock`**。「從記憶體佇列拿最新的就不用等
  flush」的誘惑很大，但那條回呼執行緒更新的價**就是他的停損**。代價是今天的最後一筆有
  ≤1.5 秒的落地延遲，**寫在 `.tk-foot` 上**。解析（一天 0.5 秒）跑在 HTTP 執行緒、
  只拿 `TICK_LOCK`。守衛：`tick-backend.py` ⑧ 用 AST 掃，負控組是「同一把尺在
  `update_state()` 裡抓得到 `state_lock`」。
- 守衛：`tools/probe/tick-tab.mjs`（**191 項**，真瀏覽器＋真滑鼠，負控組用**原始碼突變**：
  把產品函式 `toString()` 出來、刪掉那道守衛、`eval` 回去 —— 不會跟產品程式分岔，
  而且每次突變都先斷言「目標字串真的在」「內容真的變了」）
  ＋ `tools/probe/tick-backend.py`（**199 項**，離線，全程唯讀他的 `tick_logs` 並在收尾
  逐檔比對雜湊證明沒動過）＋ 合成資料產生器 `tools/probe/tick_synth.py`
  （⛔ 價格一律 12000 附近，46xxx/47xxx 會撞到他的真實紀錄；
  `synth_polled_day()` 產**合格的取樣檔**（`badts`／`strpx`／`with_k`／`all_bad_t`
  是要被擋掉或要算進 `bad` 的壞資料），`synth_polled()` 是要被擋掉的假逐筆檔，別搞混）。
  ⚠️ 2026-09-07 加取樣檔那一輪：前端 115→159→**191**、後端 100→144→**199**。
- ⛔⛔ **canvas 上的字要用「攔 `fillText`」量，不可以只掃 DOM**（2026-09-07 lab-qa 退件 M2）。
  這一頁最要命的兩條紅線都畫在 canvas 上（「不是沒行情」那句、取樣金籤），
  舊版探針只掃副標 `#tksub` ⇒ 把 canvas 的 `MISS` 常數改回「不是沒行情」**159/159 全綠**
  —— **而他早上盯的是圖不是副標**。現在 `tick-tab.mjs` 的 `canvasText()` 把那個 2d context
  的 `fillText` 暫時包一層、跑一次 `tkDraw()` 收集這一次真的送進 canvas 的字串。
  同一輪補的另一條零守衛：**「加權」那顆的 title 兩種檔必須寫不同句**（QA 併回單一句也是
  159/159 全綠）。**通則：紅線是「畫面上不可以出現某句話」的時候，量的對象就必須是畫面。**
  ⚠️⚠️ **第一版（108＋58 項）有 7 個突變打不紅，全部落在「接線／端點」那半**
  （2026-09-07 lab-qa 做了 22 個突變；跟 `tick_writer.py` 那一輪**同一類錯**：
  守衛蓋的是自己剛寫的那一側）。打不紅的是：買賣價帶整個不記錄（`elif k == "b"`）／
  路徑穿越防線失效／`/api/tick/days` 路由整個壞掉／`TICK_CACHE` 沒有上限／
  快取只比 mtime 不比 size／壞時間戳用 `sec=0` 頂替（＝**安靜地少**）。根因兩種：
  ①「探針自己重跑一次正則」＝**恆真**（`LP.re.match(...)`，跟 handler 無關）；
  ② 拿 `原始碼.index('/api/tick/days')` 當路由測試 —— 命中的是**上面那行註解**。
  **通則：端點的測試一定要真的起服務打進去**（`ThreadingHTTPServer` 綁埠 0，
  ⛔ 不是 8770），路徑穿越要驗「回 400 ＋ 目標檔案真的存在也讀不到」——
  「檔案剛好不存在所以 404」不是防線。現在這六個突變都證明過會紅（⑨／⑤b／⑥b）。
  ⚠️⚠️ **第二輪（159＋144 項）lab-qa 又找到 7 個打不紅**，一樣全在「這一輪新長出來的那半」
  ——`tick_day()` 兩行對調（M1，端點側零守衛）／canvas 的缺口文案／加權 title 併成單一句／
  取樣列壞時間戳靜靜當 08:45（＝上一輪才在逐筆側補起來的**同一個病**在取樣側重長一次）／
  `ms_med` 寫死 460／取樣嗅探拿掉「不帶 `k`」＋「讀得懂的 `t`」／快取拿掉「檔案變小就重讀」。
  現在七個都證明過會紅（②b-2／②b-3／②c／②d／⑤b CM3／⑬ FM5／⑬ FM6）。
  **通則（連兩輪同一形狀）：突變要專打「這一輪新寫的那一側」與「別人幫我做的那一側」，
  自己剛寫完的地方本來就會紅。**
  ⚠️ `tick-tab.mjs` 的 ⑫ 原本叫「不影響即時」，**那是誇大的**：治具 `fe_harness.py`
  只有 HTTP 服務、**沒有 4Hz 主迴圈**，結構上量不到「會不會拖慢停損」。
  已改名為「增量輪詢期間 `/api/state` 的回應時間」，並把 max 一起量。
  主迴圈那件事的數字在上面「不碰 `state_lock` ≠ 零成本」那條。
  ⚠️ 治具的時間戳撞號：`09:02`（缺口日開頭）與 `09:10`（`/tick/short` 的結尾）
  **剛好撞到他真實交易的分鐘**，已改成 `09:03` / `09:12`。改一邊要**兩邊一起改**
  （`tick_synth.write_gappy` ↔ `tick-backend.py` ⑥、`fe_harness` `/tick/short`
  ↔ `tick-tab.mjs` 的 `/tick/clock`）。`leak-scan.py` 對「截斷成 HH:MM」是死角
  （它自承清單第 ① 條）。
  ⚠️⚠️ **洩漏規則的適用範圍**（2026-09-07 PM 裁示；之前這裡寫「命中一律改掉、不加豁免」
  沒有界定範圍，結果同一個治具連換兩次時刻還是撞號 —— 09:05 撞、上一輪當成安全值的
  09:12 也撞）：
  「**命中一律改掉、不加豁免**」適用於 **價格／完整時間（HH:MM:SS）／賺賠點數／心得原文**。
  **只到分鐘的 `HH:MM` 不在範圍內** —— 理由三條：①他 08:45~09:30 的下單窗口只有 45 分鐘，
  而他佔了其中 **13 個分鐘**，治具隨便挑一個時刻撞到幾乎是必然，不是疏忽；
  ②一個 `HH:MM` 沒有價格／點數／方向任何旁證時，資訊量近乎零；
  ③`leak-scan.py` **本來就不掃**它。**留一條大家都不遵守、也守不住的鐵律，比沒有更糟。**
  所以 `tick_synth.py` 的 `09:03`／`09:05`／`09:12` **都不用改**。
  ⚠️ **但同一行或相鄰行只要還有價格、點數或方向當旁證，就回到「一律改掉、不加豁免」** ——
  有旁證的時候那個分鐘就不再是零資訊了。
  ⚠️ 效能負控組（不分桶）在這台機器量到 **48.7~98.2 ms/格（10~20 fps）**，
  沒有達到規格寫的 ≥100ms；判準改成「比分桶慢 ≥5 倍且 fps ≤30」，絕對值照實印出來。
  ⚠️ 注入的測試資料 `s` 要用**次秒解析度**：整數秒的話 40,000 個點只落在 2,700 個相異 x 上，
  「不分桶」那條折線退化成一堆零長度線段，負控組會量到 27ms 而且跑三次差兩倍。
### 【程式下單】分頁（四種方向判斷的模擬對照）— 2026-09-07 加

第四個分頁「程式下單」（`data-tab="auto"`，放**最右**）。規格 `tools/shioaji/AUTOTEST-TAB-SPEC.md`
（lab-ux 寫的，⚠️ 那份第 §8 的算式是錯的、§7.1 那一列數字沒量過，見下面）。

回答**一個問題，而且只有這一個**：「判斷方向」到底有沒有加分？
每個交易日 **09:03:30** 讓四種算法各記一筆**模擬**進出場，唯一的差別是方向怎麼決定：

| | 方向怎麼決定 | 訊號值 |
|---|---|---|
| **A** | 09:00 那根 5 分 K 到那一刻是漲是跌 | `P(09:03:30) − P(09:00)` |
| **B** | 08:45 開盤到那一刻是漲是跌 | `P(09:03:30) − Open(08:45)` |
| **C** | 同 B，但**超過** 30 點才做 | 同 B（`C_THRESH=30`） |
| **D** | **一律做多，完全不判斷**（對照組） | 無 |

⛔⛔ **這一頁永遠只是模擬，一張單都不會送出去，也不是自動下單的前置作業。**
Benson 2026-09-07 拍板：先只做模擬、一天 1 次、±100、他要先看一個禮拜。

- ⛔ **一行都不准 import／呼叫 `broker`**，也不打 `/api/enter`、`/api/real/*`。
  他自己那一欄是**直接讀** `real_trades/*.jsonl`（唯讀），**刻意不走 `broker.trades_history()`**
  —— 走 broker 就會讓「這一頁碰不到下單路徑」變成靠人自律、AST 掃不出來。
  守衛三道：`autotest-backend.py` ①（AST 掃後端，**不可以搜字串**：這一節的說明本身
  就在講「不准碰 broker」，搜字串會把註解當成違規）＋ 前端 DOM 掃
  （`[data-act]`／`[data-rdir]`／`form`／`type=submit` 全部 0 顆）＋
  ⭐ `autotest-tab.mjs` ⑳ 用 CDP 把這一頁**真的發出去的每一個請求**記下來，
  斷言「全程一個 POST 都沒有、也沒有任何請求打到 `/api/enter`／`/api/real/*`」
  （實測 291 個請求全是 GET）。**那是最硬的一道 —— 量的是行為，不是原始碼。**
- ⛔ **不碰 `on_tick`／佇列／`state_lock`**。09:03:30 那一刻是主迴圈（4Hz）自己從 `Today`
  讀價、**在記憶體裡組好一個 dict**，丟進 `_AUTO_Q` 給獨立的寫檔執行緒落地（`tick_writer` 那一招）。
  ⛔ **不開新的即時監控迴圈**：四條的 ±100 是**事後**從 1 分 K 算的（`_auto_settle`）。
- ⛔⛔ **主迴圈那一行一定要走 `_auto_tick_guarded()`**（try 在它裡面）。這一頁是**研究功能**，
  它出任何差錯都不可以讓主迴圈中斷 —— 中斷了就是**停損不再監控，而且畫面上看不出來**。
  ⚠️ 但**不可以安靜地吞**：`AUTO["tick_err"]` 計數 ＋ 主控台警告（前三次）＋ **畫在畫面上**。
  ⚠️⚠️ **為什麼 try 被抽成一支獨立函式**（2026-09-07 lab-qa 退件 R3）：原本 try 寫在
  `main()` 裡，探針起不了主迴圈 ⇒ 守衛只能**比字串**（「呼叫點前後 200 字裡有沒有
  try/except/tick_err/print」）。lab-qa 把 `AUTO["tick_err"] = …` 換成 `pass`
  （`"tick_err"` 這個字串仍在窗口裡）⇒ **125/125 全綠**，
  但真實行為是 except 區塊自己丟 `KeyError` ⇒ **例外衝出 try ⇒ 主迴圈當場中斷**。
  **出貨的程式本身是對的，錯的是守衛。**
  現在 ⑩ 是**行為測試**：真的把 `_auto_tick` 換成會丟例外的版本，斷言
  「呼叫端沒被打斷 ＋ `tick_err` 真的加了 ＋ 真的印了字 ＋ `AUTO["err"]` 記下來了」，
  再配 AST「`main()` 有呼叫 `_auto_tick_guarded`、**而且沒有**直接呼叫 `_auto_tick`」。
  ⛔ 不要為了少一層呼叫把 try 搬回 `main()` —— 搬回去守衛就退化成字串比對。
  代價量過了：`_auto_tick` **0.738 µs/圈** → `_auto_tick_guarded` **0.766 µs/圈**
  （多 0.028 µs；4Hz ⇒ 一整天多 0.0095 秒）。
  ⚠️ `AUTO["tick_err"]` 要在 `AUTO` 的初始 dict 裡就是 0，⛔ 不要寫 `AUTO.get("tick_err", 0)`
  （那會讓「有沒有真的在數」變成看不出來的事，而且 except 區塊自己丟例外時最致命）。
- ⛔ **`AUTO["gaps"]`（`quote_gaps`）是「那個早上有幾秒沒報價」的唯一痕跡**，
  永遠 0 也看不出來 ⇒ 那就是本專案明令禁止的「安靜地少」。
  lab-qa 把 `AUTO["gaps"] += 0.25` 拿掉 ⇒ 125 全綠（退件 R4）。
  守衛：⑩ 的行為測試（`last_recv` 很舊時跑 4 輪要累加 1.0 秒；報價是新的一秒都不准加）。
- ⛔ **`err` ＋ `tick_err` 一定要上畫面**（退件 R5）：後端一直有在數、console 也有警告，
  但 **console 只印前 3 次、而他不會去看主控台** ⇒ 對「看畫面的人」來說還是吞掉了。
  現在 `auto_days()` 與 `auto_stats()` 都端出 `err`／`tick_err`，前端畫在
  ledger 那一行旁邊（`atNotesHTML()`，含「停損不受影響」那句）。守衛：`autotest-tab.mjs` ㉒。
  實測（`tools/probe/autotest-loop.py`，獨立 4Hz 治具、一年份 240 天資料）：
  停損檢查週期 **熱 253 ms／冷解析 253 ms**，對照組 `/api/bars` 換日 **323 ms**、
  什麼都不打的基準 272 ms ⇒ **這一頁不是新洞**。（基準比較慢是 Windows 計時器精度，
  有 HTTP 執行緒在跑時 `sleep(0.25)` 反而更準，不是量錯。）

**09:03:30 那一刻的價從哪來**（⛔ 拿不到就不要編）

| 要什麼 | 從哪來 | 拿不到的時候 |
|---|---|---|
| 進場價 | `Today.price` —— **就是停損看的那個價**（⛔ 不用 `STATE["chips"]`：那是渲染快照、還要拿鎖） | 那天只寫一列 `miss`，`why=no_quote` |
| 買賣價 | `Today.bid` / `Today.ask` | 留白 |
| 08:45 開盤 | `Today.open`（盤中重啟時 `seed_from_bars()` 會補回來） | B/C 的訊號寫 `null`、`dir=None` |
| 09:00 的價 | `Today.minute_bar[540]["o"]` ＝ **09:00 那一分鐘第一筆成交**（正是 5 分 K 的開盤） | 退到 `minute_close[539]`（08:59 最後一筆），並把用了哪一種記進 `ref.p0900_src` |

⛔ 三種情況一律**不寫 sig，只寫 miss**：沒有成交價（`no_quote`）／報價超過 5 秒沒更新
（`quote_stale`）／只有中價還沒有成交（`mid_only`）。
⛔ 第四種是 **`late`**：主迴圈發現「跨過 09:03:30 了」時如果已經晚超過 `AUTO_LATE_MS`（3 秒），
**那一刻的價不是 09:03:30 的價** —— 面板 09:10 才開起來、或看門狗剛重啟就是這樣。
**拿它記進去，那筆成績是假的而且看不出來。**

**資料落地**：`tools/shioaji/autotest/YYYY-MM.jsonl`（**已 gitignore**），`open("a")` 只 append。

- ⚠️ 因為只能 append，「結算」不是回頭改那一列，而是**再 append 一列 `rec:"settle"`**，
  讀檔時以 `date` 為鍵合併。三種列：`sig`／`settle`／`miss`。
- ⛔ **記數值不只記方向**（`sig.A` / `sig.B` 是點數）＋ 能記的中間值全記
  （`ref.open0845/p0900/prev_close/hi/lo/rng20`、`bid`/`ask`、`at_lag_ms`、`quote_gaps`）。
  兌現：門檻掃描（§10）直接從 `(sigB, ptsB)` 重算任何門檻的成績，**不用再等二十天**。
  `rng20` 是在寫檔執行緒**事前**算好的（`_auto_warm`）—— ⛔ 09:03:30 當下不准跑 pandas。
- ⛔ **每一列都要有去處**：`sig + settle + miss + dup + bad ＝ 檔案總列數`
  （`auto_stats().ledger`，畫面上直接印出來）。一列壞資料只准弄掉那一列。
- **快取鍵是 `(mtime, size)`**，⛔ 只比 mtime 不夠（同一秒續寫會讓畫面停在舊資料）。

**結算**：`_auto_settle()` 用 1 分 K，**從標籤 `09:04` 那根（含）開始看，比較一定是 `>=`**。

- ⛔⛔ **`one_min_bars()` 的標籤是「起始時間」**（`to_timeframe()` 已經往回挪一分鐘，
  見上面「1 分 K 用結束時間標記」那條）⇒ 標籤 `09:03` 那根涵蓋 **09:03~09:04**、
  標籤 `09:04` 那根涵蓋 **09:04~09:05**。要排掉的是 `09:03` 那根（它的高低含 09:03:30
  進場前的價，用它會把「進場前就摸到」算成觸價）；`09:04` 那根**整根都在進場之後、
  本來就該收進去**。
- ⚠️⚠️ 舊實作寫成 `b["t"] > AUTO_SETTLE_FROM`（2026-09-07 lab-qa 用真 csv 實測退件 R2）
  ⇒ **實際用到的第一根是 09:05**、看不到觸價的變成 **90 秒**（不是 30 秒），
  而畫面副標寫「出場從 09:04 的 1 分 K 開始算」**是一句假話**。
  ⚠️ 這一條可回復：`_auto_read` 是**後寫的 settle 蓋前面的**，修好之後重新結算就正確了。
- 代價（修好之後的正確說法）是 **09:03:30~09:04:00 這 30 秒**的觸價看不到
  （要在那半分鐘走 100 點）—— `settle_from` 欄位有記，副標寫「出場從 09:04 那根
  1 分 K（含）開始算」。強制平倉是**當天日盤 13:45**（PM 拍板）。
- 守衛：`autotest-backend.py` ⑤b —— 攔 `_auto_run` 收「settle 真的拿到哪幾根」，斷言
  **第一根標籤 == 09:04**、`09:03` 不在裡面、`09:04` 在裡面，而且**寫進 `settle_from`
  的值要跟真的用到的第一根一致**（副標才不會又變成假話）。負控組是改回 `>`。
- ⛔ **同一根同時摸到 ±100 ⇒ 保守算停損**並標 `both`，那個筆數要顯示在畫面上。
- ⚠️ **`eod`（「13:45 收盤平」）實際拿到的是 13:44 的價**，少一分鐘 ——
  `one_min_bars()` 的最後一根是 `13:43`（既有行為，不是這一輪造成的，
  詳見上面「1 分 K 用結束時間標記」那條）。⛔ 畫面上不要寫「13:45 的價」。
- ⚠️ **另一把尺不要搞混**：σ 那段（下面）量的是**原始 csv** 的 `hm` 欄位，
  那是永豐給的**結束時間**標籤 ⇒ 在那段裡「`hm == 09:04`」指的才是涵蓋 09:03~09:04 的那根。
  兩把尺同時存在於這個檔案裡，改任何一邊之前先確認你在看哪一把。

**⛔⛔ 結算不准跑太早：`eod` 的前提是「日盤真的收了」**（2026-09-08 修，他早上真的踩到）

上線第一天早上 09:05 他打開分頁 ⇒ `auto_days()`／`auto_day()` 順手排了一次結算 ⇒
`_auto_settle` 手上只有兩根 K 棒（09:04／09:05）⇒ `_auto_run` 沒摸到 ±100 ⇒ 回 `eod`
⇒ **拿 09:05 的收盤價當「收盤價」寫死**。畫面上四條泳道全部是「09:05 收盤平 ±67 點」——
**一筆假成績，而且長得跟正常的一模一樣**。他真實那一列是 `bars:2`、`wrote_at 09:05:29`。
更糟的是舊版 `if … or r.get("runs"): return` 讓它**只算一次** ⇒ 錯的結果**被鎖死**。

- 根因不是 `_auto_run` 有 bug，是**語意錯置**：`eod` 的意思是「**到收盤都沒碰到**」，
  這個前提在盤中不成立。⛔ 通則：**一個分支的語意有時間前提時，那個前提要被檢查，
  不能靠「呼叫的人應該不會在盤中叫我」。**
- 現在的規則（`_auto_settle`）：
  - **摸到 ±100** ⇒ 立刻結算（不管幾點，那是最終結果）＋ `final: true`
  - **沒摸到、日盤還沒收** ⇒ **一列都不寫**，畫面維持「持倉中」
  - **沒摸到、收盤了** ⇒ 才寫 `eod` ＋ `final: true`
- **「那一天結束了沒」＝ `_auto_day_over(d)`，站在那一天的角度**：過去的日子一律已結束
  （照樣立刻結算）；只有 `d == 今天` 且時鐘 < `AUTO_SETTLE_AFTER`（13:47）才擋。
  **用本機時鐘**（`datetime.now()`）——`_auto_settle` 跑在寫檔執行緒上，`STATE["clock"]`
  要拿 `state_lock`（鐵律二禁止），永豐的 tick 時間只活在 `on_tick` 那條執行緒的 `Today` 裡；
  而且這一頁的「今天／到了沒」**本來就全用本機時鐘**，再開第二把尺就是這個檔案裡
  「兩把尺」那類事故的溫床。
- **第二道：`AUTO_EOD_LAST`（`13:43`）**。日盤收了、但手上這批 1 分 K 只到 10:00
  （永豐給不完整）照樣會寫出「10:00 收盤平」⇒ 最後一根的標籤要到 `13:43` 才准寫 `eod`。
  ⚠️ `13:43` 不是筆誤，`one_min_bars()` 的最後一根就是它（既有缺口，見上面那條）。
- **`final` 欄位＝「這一列可不可信」**。⛔ **沒有 `final` ＝ 2026-09-08 之前的舊格式
  ＝ 不可信的早結**，`_auto_read_month` 讀完會把它的 `runs` 清成 `None`
  ⇒ 成績表／累計圖／配對卡／日期清單／今天卡**一個字都拿不到它**，
  而 `_auto_settle` 會當成沒結算過、重算一列 append 上去（後寫的蓋前面的）。
  ⛔ **帳本不動**：那一列還在檔案裡，`sig+settle+miss+dup+bad ＝ 總列數` 照樣成立。
- **`_auto_put` 的結算冷卻鍵要帶「日盤收了沒」**（`(date, over)`）。只用日期的話，
  盤中排過一次之後 **13:47 主迴圈那一次會被冷卻擋掉**，要再等 5 分鐘才修得好。
- 畫面：**「持倉中」與「結算中」是兩個狀態，⛔ 不准寫同一句**。持倉中＝還沒摸到 ±100
  且日盤還沒收（正常的等待）；結算中＝該算了卻還沒算出來（要注意）。
  ⛔ **持倉中只寫狀態＋進場價，不算浮動損益**（規格 §16-3 拍板：會跳的模擬損益是這一頁
  最容易讓他開始盯盤的東西）。⛔ C 沒過門檻那幾天寫「這天不下單」——
  它沒有部位，寫「持倉中」是假話。
- 守衛：`autotest-backend.py` ⑤c（**六條各自有負控組**：拿掉時間判斷 ⇒ 真的寫出 `eod`／
  把 `AUTO_EOD_LAST` 放寬到 `00:00` ⇒ 真的寫出「09:05 收盤平」／舊格式那一列補上
  `final:true` ⇒ 那份假成績就端得出來了）＋ `autotest-tab.mjs` ⑪b（DOM ＋ **canvas** 兩邊，
  負控組是把 `AT.data.runs` 灌成當天那個 bug 的形狀 ⇒ 兩邊都冒出「收盤平」）。
  ⚠️ 治具（`at_harness.py`）的 `_auto_day_over` **要跟著假時鐘**，否則同一份治具
  13:47 之前跑跟之後跑會畫出不同的畫面。

**天花板 ＝ z·σ/√n（z ＝ 1.96 ＋ 0.84 ＝ 2.80）**

- ⛔ CLAUDE.md 開頭那個 **σ=89.7 是另一套出場口徑的，不可以混用**。這一頁是 ±100 停利停損。
- lab-dev **自己重算過**（規格寫 96.8，⛔「不可以放沒量過的數字」）：`tmf_1min.csv` 最後
  **505** 天、進場價用標籤 09:04 那根的 Close 當 09:03:30 的代理、一律做多、±100 觸價、
  都沒摸到就 13:45 平、不扣費 ⇒ **σ = 96.61**（510 天全樣本 96.65）、tp 231／sl 239／eod 40。
  程式裡是 `CEIL_SIGMA = 96.6`。⇒ 天花板(505)=**12.0**、(20)=**60.5**、(120)=24.7。
  `autotest-backend.py` ② 每次都用同一套算式重算一遍，對不上就紅。
- **「證不出來帶」的半寬 ＝ 天花板(n)×n ＝ z·σ·√n**（`band_k = 270.5`）。⛔ 刻意跟天花板
  同一個 z 與 σ —— 「線還在帶子裡」⇔「每筆沒超過天花板」，圖跟表必須是同一句話。
  ⚠️ 規格叫它「95% 帶」，**那個名字是錯的**（95% 是 1.96σ）；畫面上叫「證不出來帶」。
- **`total_n`（累積到今天共幾天）與 `n`（目前窗口幾天）是兩個數字**：進度尺用 `total_n`
  （用 `n` 的話按「近10」進度尺會縮回去，看起來像資料不見了），天花板那一行用 `n`（它貼在成績表底下）。

**⚠️⚠️ 規格 §8 那個配對判準的閉合式是錯的，不要照抄**
`need = ceil((m + 1.96√m)/2)` 是**沒有連續性修正的常態近似**：m=5~40 有 **17 個**跟精確二項式
對不上，而且**每一個都偏小**（m=8 給 7、精確是 8）—— 也就是它會**提早**說「算數了」，
而這一頁存在的理由正是防這件事。現在用精確二項式（`auto_pair_need`，m 很小、成本可忽略）。
⚠️ **m ≤ 5 時全贏也不算數**（`P(X≥m)=0.5^m > 2.5%`），回傳的 need 會大於 m，
畫面上照實寫「這 m 天全贏也還不算數」，⛔ 不可以夾到 m。

**⛔⛔ 名字：畫面上不准出現 A／B／C／D（規格 §2.0，被 Benson 退件過一次）**

> 他的原話：「**我要從哪裡知道現在我看的是哪個做法？**」
> 根因不是標得不夠多（三處都有標），是**標的是代號，他得記住 A 是哪個**。

| 資料層 key（⛔ 不變） | 畫面上的名字（⛔ 唯一寫法） | 名字底下那行小字 |
|---|---|---|
| `A` | **5 分 K** | `09:00 起算` |
| `B` | **開盤起** | `08:45 起算` |
| `C` | **開盤起・要 {C_THRESH} 點** | `08:45 起算・不夠就不做` |
| `D` | **不判斷** | `一律做多（基準）` |

- 前端只有 `AT_NAME`／`AT_SUB`／`atName()`／`atSub()` 這一組（`AT_ORDER === ATKEYS`）。
  `C` 的名字**自帶門檻值**（改成 80 就變「開盤起・要 80 點」，正本是後端常數 `C_THRESH`）
  ⇒ ⛔ 不准把字串散在各處硬寫。「換一個門檻看看」那支滑桿 ⛔ 不准改動任何地方的名字。
- 必須用名字的地方：**今天卡抬頭／成績表第一欄＋說明小字／泳道／累計圖線尾／配對卡／
  天花板那行／日期清單每一列／〈這一頁在算什麼〉那十條／門檻掃描的說明**。
  ⛔ 不准縮寫（「開盤・30」「開30」都不行）、⛔ 不准留「A＝5 分 K」這種對照說明
  （需要那行字就代表命名失敗了）。表頭第一欄叫**做法**，⛔ 不叫「算法／策略／模型」。
- **泳道左邊是「名字欄」，寬度用量的**（`atLaneLayout()`）：對四個名字各跑一次
  `measureText`（`600 11px` sans），`L = ceil(max) + 18`（實測桌面 **L = 103px**）。
  K 棒與泳道**共用同一個 `xOf()`**（`gx = L + 4`），±100 的兩張籤右對齊在 `L − 8`。
  ⛔ 不准寫死 px（門檻變三位數名字就變寬）。`L > 繪圖區 × 0.25` 時切成**堆疊排法**
  （名字自己一列、列高 22→30、`.at-wrap` 在 ≤720px 換成 `aspect-ratio:520/620`）——
  ⛔ **放不下不是縮寫的理由，是重新排版的理由。**
- 累計圖的線尾標籤放不下時（名字約 90px、右邊留白只有 64px）**往左貼齊畫布右緣
  ＋ 字底下鋪一塊底色**，⛔ 不准縮寫、不准改回字母。
- ⚠️⚠️ **上一輪這一整塊是零斷言**（2026-09-07 lab-qa 退件 R1）：實作用
  `ATRULE` ＋ `<b>k</b>`、畫面上 DOM 21 個 ＋ canvas 12 個獨立代號，
  探針照樣 **134/134 全綠**；`atName`／`AT_NAME` 在原始碼裡零命中。
  現在守衛是 `autotest-tab.mjs` ㉑：①四個名字各自出現（DOM 與 canvas **各驗一次**）、
  ②孤立代號正則 `(^|[^0-9A-Za-z])[ABCD]([^0-9A-Za-z]|$)` 在 DOM ＋ 兩張 canvas 上
  **零命中**、③四處同名同序、④成績表說明小字的文案（lab-qa 把它清空過 ⇒ 134 全綠）、
  ⑤名字欄寬 `L >= ceil(max 名字寬) + 18`。負控組是把 `atName()` 改成回傳 key。

**⛔ 文案／常數那一側（修完退件後 lab-dev 自己往那個方向再找一輪，抓到四個）**

1. **hover title 裡的數字不准寫死**：`#attrack` 的「505 筆 ≈ 兩年」與 `#atceil` 的
   「505 筆的時候這條線是 12.0 點」原本寫死在 HTML 裡 ⇒ `CEIL_REF_N`／`CEIL_SIGMA` 一改，
   那兩句就是假話，**而且只有 hover 才看得到，永遠不會有人發現**。
   現在由 `atPaintStats()` 依後端端出來的 `track_n` 算好寫進去；守衛把 `track_n`
   改成 333 驗它真的跟著變。
2. **畫面上的「時分秒」只准有 `signal_at` 一個**：09:03:30 已經從 04:30 改過一次，
   散在 `ATWHY`／空狀態／canvas 金籤裡的時刻字串會在下一次改動時**整批變成假話**。
   守衛掃 DOM ＋ 兩張 canvas 的所有 `\d\d:\d\d:\d\d`，除了後端的 `signal_at`，
   其餘**只准出現在〈這一頁在算什麼〉的歷史對照**那一條（「09:03:30 沒有被證明比
   09:04:30 好」）。
3. **常數自洽**（`autotest-backend.py` ⑫b）：`SIGNAL_SEC == SIGNAL_AT`、
   **`AUTO_SETTLE_FROM` 必須是 `SIGNAL_AT` 的下一分鐘**（訊號時刻改了卻忘了改窗口 ⇒
   窗口會含進場**前**的 K 棒 ⇒ 把「進場前就摸到」算成觸價，畫面上完全看不出來 —— 這就是 R2）、
   `AUTO_TP/SL is TP_POINTS/SL_POINTS`、`track_n == CEIL_REF_N`、端點端出去的
   `thresh／rate_min_n／cum_min_n／sigma／z` 都等於常數；前端 ㉓ 再驗一次**前後端不分岔**。
4. **文案表不准被掏空、兩個不同的原因不准寫同一句**（`ATWHY`／`ATEXIT`／`AT_NAME`／`AT_SUB`
   ＋〈這一頁在算什麼〉那十條的**內容**，不是只數條目數）。寫同一句一定有一句是假的
   ——【細節】的「加權 title 兩種檔寫同一句」就是同一個病。

**版面（PM 2026-09-07 裁示，v1 被 Benson 退件過一次）**

> 他對 v1 的原話：「聽不懂，然後我覺得面板一大堆多餘的文字」「你不用管甚麼成不成校，
> 只須要讓我看結果根數字就好」

- **第一屏 ＝ 圖＋四條泳道 ／ 成績表。就這樣。**
  ⚠️ 舊版是「進度尺 ／ 圖 ／ 成績表」三樣，2026-09-08 進度尺被 Benson 拿掉之後剩兩樣。
- ⛔ 摺疊區**預設全部關著**：配對對照卡、門檻掃描。
  ⛔ **不准把裡面任何一條搬回平鋪畫面**，那正是退件的原因。
  ⚠️〈這一頁在算什麼〉那個摺疊區已於 2026-09-08 整個刪掉（見下面那一節）。
- **「今天」那張卡收進日圖卡底下的四格讀數**（`.at-today`）：它同時是泳道的圖例。
  規格原本是獨立一張卡，⚠️ 那樣第一屏就是四樣東西 —— PM 裁示「就這三樣」。
- **`.simlock`（⚪ 模擬 · 不送單）全頁恰好 1 顆**，掛在日圖卡標題旁（最容易被截圖的地方）。
  ⛔ 0 顆與 2 顆都算紅（v1 掛三處是退件原因之一）。金色在這個面板既有的意思是
  「這是什麼種類的資料，注意」（＝【細節】的取樣金籤），沿用不是新增意思。
- ⛔ **顏色只給損益，比別頁嚴格**：方向、訊號值、筆數、門檻、天數、天花板一律中性色。
  「做多」染紅會讓全站唯一一個「程式現在說了什麼方向」的地方看起來像興奮的訊號。
- ⛔ **時態鎖**：`atSigPassed()` 一律用**後端時鐘**（`/api/auto/days` 的 `now`），
  瀏覽器時鐘只用來算「從拿到那一刻起過了幾秒」。09:03:30 之前不顯示方向、不倒數、不預告。
  ⚠️ 所以 `auto_day()` 對「今天但還沒記錄」**要回得出東西**（`px:None`），⛔ 不可以回 404 ——
  回 404 的話畫面會退到「最後一個有紀錄的日子」，而那一天**有方向**⇒ 09:03 之前站在這一頁
  看到的是別天的方向，最容易被讀成「今天的判斷」。那才是不預告這條紅線真正的破口。
- ⛔ **不做多數決／共識／「四個裡有幾個看多」** —— 那就是訊號強度。
- ⛔ **少樣本不給百分比**（`RATE_MIN_N=30`）、**少於 10 筆不畫累計線**（`CUM_MIN_N=10`）。
  ⚠️ 這道門檻**前後端各有一道**（後端 `rate` 直接回 `null`）⇒ 只拿掉前端那道**打不紅**，
  探針的負控組要「拿掉門檻 ＋ 在前端自己算」兩件事一起做。
- **泳道**：四條在同一時刻、同一個價進場 ⇒ 進場標記完全重疊、出場價又只有三種
  ⇒ 價格區只畫**一個**進場標記（畫四個是假的）＋ **兩條** ±100 線
  （⛔ 不准標「停利／停損」：同一條線對做多是停利、對做空是停損，標了一定有一邊是錯的），
  身分全部下放到泳道。⛔ **C 不做的日子畫成虛線 ＋「這天不做」＋ 訊號值，不可以整條消失**。
  ⛔ **D 跟 A/B 同向的日子泳道就是長得一樣 —— 那是事實不是 bug，不要合併**
  （合併掉的話「判斷有沒有加分」正好看不見）。
- **預設畫整個日盤 08:45~13:45**（±100 大多數日子 09:30 前根本摸不到，只畫 45 分鐘的話
  四條泳道全部跑出畫面右邊）；`只看 08:45~09:30` 是一顆預設 off 的 chip，出場在畫面外時
  色帶拉到右緣 ＋ `▶` ＋「畫面外」。09:30 那條虛線的意思是**他自己收手**，
  ⛔ 不是模擬單的出場條件。
- **價格軸 `niceStep()` 貼齊，⛔ 沒有遲滯**：這張圖的視窗只有兩種固定值、資料只增不減
  ⇒ 遲滯的 early-return 結構上進不去（跟【細節】同一個坑）。⛔ 不要抄一段不承重的程式回來。
- ⛔ **±100 與交易標記都不准撐大價格軸**（畫不到就掛邊緣籤）—— 也因此完全不必碰真實單的
  `exit`（它可能是 `null`，`Math.min(lo,entry,exit)` 會把它當 0）。
- ⛔ **時間軸只印 `HH:MM`**（印到秒會讓人以為在看【細節】那張秒級圖）。
- ⛔ **canvas 的疊放順序是三層，而且只有截圖看得出來**（2026-09-07 兩個都是這樣抓到的）：
  ① 09:03:30 的金線與 ◆ → ② **他自己那幾張籤**（每張往下錯開 19px）→
  ③「09:03:30 模擬進場 12010.3」那張金籤（**最上層**）。
  理由：他的進場時刻本來就落在 09:03:30 附近、價也差不多 ⇒ 不錯開就互相壓住、
  順序反了那個 ▲ 會蓋掉金籤上的字。**這一頁的主角就是那一筆。**
  同一輪還抓到「時間軸畫在 `x−16`，x=0 那格的 `08:45` 被切成 `45`」。
  ⚠️ **這兩個掃字串都掃不到**（送進 canvas 的字串完全正確）⇒ 探針的 `canvasDraws()`
  連 **x/y/寬度**一起收，斷言「沒有一段字被畫到畫布外」「他自己那幾張籤的 y 兩兩不同」
  「真 那幾張在 模擬進場 之前畫」。**量畫面就要連座標一起量。**

**⛔⛔ 2026-09-08 他看實機之後拿掉的四樣東西 —— ⛔ 一樣都不准回來**

> 他的原話：「程式下單那邊這個欄位不需要，然後這個線的面板可以改的小一點點嗎，
> 我覺得他現在有一點點大。然後我自己的這邊都拿掉，下面這個我根本看不懂是在幹嘛的。
> 然後這一頁在算甚麼也拿掉，我不需要看這個我也不會看」

| 拿掉的 | 原本是 | 後端 |
|---|---|---|
| **進度尺**（`#attrack`／`atTrackHTML`） | 第一屏最上面那條「這個測試跑到哪裡了 1 / 505 筆」 | `track_n`／`total_n` **照端**（`track_n` 還在算天花板 title 的數字） |
| **「你自己」那兩列**＋上面那條分隔＋累計圖那條金線 | 成績表最下面兩列 | `_auto_mine_day`／`_auto_mine_rows`／`stats.mine` **照算** |
| **帳本那一行** | 「同一根同時摸到 ±100 … 檔案 N 列＝訊號＋結算＋沒錄到＋重複＋讀不出來」 | `ledger` 與 `sig+settle+miss+dup+bad ＝ 總列數` 不變式**照算** |
| **〈這一頁在算什麼〉**（10 條） | 最下面那個摺疊區 | — |

- ⛔ **拿掉的是畫面，不是資料**。三道既有守衛一條都沒有放寬：`⑧c`「只讀不寫」（AST ＋
  暫存 real 目錄比雜湊）、帳本不變式（`autotest-backend.py`）、`autotest-tab.mjs`
  ⑫（歷史回填要講出來）與 ⑲（讀不出來要講出來）。
- ⛔ **「拿掉帳本」不等於可以安靜地少**。`atNotesHTML()` 現在分兩種：
  **常態統計**（帳本五項總和／「13:45 收盤平 N 筆」／「持倉中 N 筆」）⇒ 不畫；
  **異常**（同一根同時摸到 ±100／還沒結算／沒有記錄的天／歷史回填／讀不出來／重複／
  `tick_err`）⇒ **只在 > 0 時出現**，正常情況整行是空的（`.at-notes:empty{display:none}`）。
  ⛔ 不要改回「無論如何都印一行」，也 ⛔ 不要把那幾個異常一起刪掉 —— 守衛 ㉔b 兩邊都在守。
- **日圖 `aspect-ratio` 1040/470 → 1040/380**（高度 −19%；1440px 視窗實測 canvas
  631 → 510px、價格區 493 → 372px）。⛔ **只有價格區變矮**：四條泳道是從下緣往上排的
  固定像素（4×22 ＋ `ATBOT` 26），名字欄寬 `L` 仍是 `measureText` 量出來的（實測仍是 103px）
  ⇒ ⛔ 不准改成縮泳道列高／縮名字字級來換高度（規格 §9.3，被退件過）。
  ⚠️ `.at-cwrap`（累計圖 1040/300）與 ≤720px 的堆疊排法（520/620）**刻意沒動**。
- **副標那句「你自己 N 筆」改叫「真實單 N 筆」**：它講的是**圖上那幾張「真 ▲ 多」籤**
  （那些**要留著**，⑬ 有斷言在守它們不互相壓住），跟被拿掉的成績表兩列不是同一件事 ——
  但留著「你自己」四個字會讓 ㉔ 那道零命中的尺分不出來。
- 守衛（**每一條都有負控組：把東西加回去要真的紅**）：`autotest-tab.mjs`
  **⑤**（進度尺：`#attrack` 不存在 ＋ 那幾句話在 DOM ＋ 兩張 canvas 零命中 ＋ 後端欄位還在）、
  **㉔**（三樣的零命中 ＋ 成績表恰好四列 ＋ `ATLINES` 只剩四條 ＋ `#atabout` 不存在 ＋
  摺疊區只剩兩個）、**㉔b**（常態不寫／`both>0` 時照寫）。
  ⚠️ 負控組還原時**要先戳破 `setEl()` 的 `e.__html` 快取**，不然 `atPaintStats()` 沖不掉
  探針注入的節點（第一版就是這樣紅了三條）。
- ⚠️ 兩條既有斷言是**改對不是刪掉**：① ⑨「進度尺不是紅綠」⇒ 改成「整頁只有掛
  `.up/.down` 的元素可以是紅綠」（範圍反而變大）；② ㉓⑤ 的時分秒豁免（原本只准出現在
  〈這一頁在算什麼〉的歷史對照）⇒ **收緊成「除了後端的 `signal_at`，一個都不准有」**。
- ⚠️ ⑰ 的 `aspect-ratio` 斷言與 `.at-wrap` 的 CSS 是**兩份數字**，改一邊要兩邊一起改。

**他自己那一欄（§11）**（⚠️ 2026-09-08 起**前端不畫**，見上面那一節；下面講的是後端口徑）：⛔ **一定要兩列**。他一天 0~3 筆、時刻不固定、有手動平倉、
`points` 可能算不出來 ⇒ 只給「全部」那一列會做出**錯誤的自我評價**，而 CLAUDE.md 說
「他自己的判斷有沒有價值」是唯一還沒被否定的假設，把它評價錯的代價比其他四條都高。
「同口徑」＝**只取當天第一筆、而且 `why ∈ {tp, sl}`**（±100 真的跑完）。
算不出點數的要**明講排除幾筆**。這一欄**只讀不寫**。

- ⛔⛔ **「只讀不寫」要有機器守衛**（2026-09-07 lab-qa 退件 R6）：舊版只有 ⑭ 那道
  「收尾比對 `real_trades/` 的雜湊」，可是**探針早就把 `AUTO_REAL_DIR` 導到暫存區**，
  ⑭ 比的是**真正的**資料夾 ⇒ lab-qa 把 `_auto_mine_day` 改成 append 一列進去，⑭ **抓不到**。
  現在 ⑧c 兩道一起做：① **AST 傳遞閉包**（從 `_auto_mine_day`／`_auto_mine_rows` 出發
  可達的函式裡，`open`／`write*`／`mkdir`／`unlink`／`_auto_append` 等寫檔呼叫**零命中**，
  負控組是同一把尺在 `_auto_append` 抓得到）；② 對**暫存的** real 目錄前後比雜湊
  （加一個金絲雀檔證明那把尺是活的）。**通則：探針把某個路徑改指到暫存區之後，
  原本守著真實路徑的那道檢查就對產品程式失效了 —— 兩份都要守。**

**守衛**（每一條都有負控組）

```
tools/probe/autotest-backend.py   203 項，離線、⛔ 唯讀 real_trades/ 與 autotest/（收尾比對雜湊）
tools/probe/autotest-tab.mjs      223 項，真瀏覽器；先起 tools/probe/at_harness.py（8773／控制埠 8774）
tools/probe/autotest-loop.py      獨立 4Hz 治具，量「會不會拖慢停損」（fe_harness 沒有主迴圈，量不到）
tools/probe/autotest_synth.py     合成資料產生器（⛔ 價格一律 12000 附近；出場用產品的 _auto_run 算，資料一定自洽）
```

- **突變測試 21（後端）＋ 12（前端）全紅**，而且**專打接線那一側**（主迴圈沒呼叫
  `_auto_tick`／`AUTO["started"]` 沒打開／端點順序對調／切分頁沒呼叫 `atEnter`／
  `#tab-auto` 沒被顯示出來／`atPaint` 沒真的畫圖）——
  tick_writer 與【細節】連兩輪的教訓都是「守衛蓋的是自己剛寫的模組，接線那半沒人守」。
- ⚠️⚠️⚠️ **第三輪 lab-qa 點名的型態（連三次同一形狀，這次換了一個方向）**：
  「這一輪的守衛專打了**接線**（做得很好，全紅），但**完全沒守文案／命名／常數那一側**
  —— R1（命名）／R4（`gaps` 常數）／R8（成績表說明文案）三個退件都在那裡。」
  ⇒ 通則：**接線＝『有沒有接上電』；文案／命名／常數＝『接上去的是不是對的東西』。**
  後者的錯**畫面照樣長得好好的**（清空一段小字、把名字換成代號、把某個常數改掉），
  所以更需要機器守衛。下一輪寫守衛時，兩側各列一次清單再開始寫。
- 2026-09-07 修完退件後 lab-dev 重做的突變（**15 個全紅，含 4 個上一輪打不紅的**）：
  後端 7（`tick_err` 換 `pass`／整層 try 拿掉／`main()` 繞過守衛直接呼叫 `_auto_tick`／
  `gaps` 不累加／`_auto_mine_day` 偷寫檔／結算窗口改回 `>`／讓探針當場爆炸）、
  前端 8（說明欄清空／今天卡・成績表・泳道・累計圖各自改回代號／名字欄寬寫死 40px／
  `tick_err` 不上畫面／讓探針當場爆炸）。每一個都印得出總結行與具名 FAIL。
- ⚠️ **突變測試只數 `FAIL` 行會有假綠燈**：突變讓探針**當場掛掉**（例外冒出來）時
  一行 FAIL 都沒有，會被記成「打不紅」。第一版就這樣把兩個突變記成綠。
  兩道處置：突變工具看「有沒有印出總結」，探針自己也把例外接住轉成一項**具名**的 FAIL。
  ⚠️⚠️ **第二道當初只做在前端、後端那支根本沒裝**（2026-09-07 lab-qa 退件 R7：
  M7／M8b 讓 `autotest-backend.py` 當場掛掉、0 行 FAIL、也沒印總結）。
  現在**兩支都有**：`autotest-backend.py` 用 `sys.excepthook` ＋ `_summary()`、
  `autotest-tab.mjs` 用 `process.on("uncaughtException"/"unhandledRejection")` → `bail()`，
  兩邊都會印出「⛔ 探針自己掛掉了（未捕捉的例外）」這一項 FAIL ＋ 總結行。
  **寫「已經做了兩道處置」之前，要對每一支探針各驗一次。**
- ⚠️ **換日那兩道守衛是互相備援的**（流水號 ＋ `if(d===AT.date)`）：只拿掉一道**打不紅**，
  探針要先證明「只拿掉一道還是綠」再兩道一起拿掉 —— 不然下一個人會以為流水號沒用。
- ⚠️ `tick-tab.mjs` 的效能負控組（不分桶要比分桶慢 ≥5 倍）**對機器負載很敏感**：
  同時有別的治具與 Chrome 在跑時量到 3.3~4.9（**分母**被拉高，分子 47~53ms 很穩），
  機器安靜時 5.6~7.2。⛔ 不要因為這個去放寬那條門檻，先把別的東西關掉再量。
- ⚠️ 加了第四顆分頁之後，`tick-tab.mjs` 那條「頂列三顆分頁」要改成四顆
  （已改；⛔【細節】仍然必須在第 2 顆，下一條在守）。

**刻意沒做的**

- ⛔ **不用 `tmf_1min.csv` 回填歷史**（規格 §16-5 的 (b)，lab-ux 推薦、lab-dev 採用）：
  回填的資料**永遠不能跟實跑相加**（分鐘資料切不出 09:03:30），畫面上要一直維護兩套數字，
  換來的只是一條無法下結論的線。**但 `src` 欄位與 `live/backfill` 的過濾機制已經做好**
  （端點 `?src=`、統計分開、探針有斷言），哪天 PM 要回填不必改架構。
- 不做浮動損益（§16-3 的 (b)，PM 推薦）：一個會跳動的模擬損益數字是這一頁最容易讓人
  開始盯盤的東西，而它沒有任何用途。結算完成前寫「結算中」。
- 不做第五、第六種算法（每多一種，「四條裡總有一條看起來很好」的機率就上升一階）。

- **永豐 SDK 會在斷線時把整個行程帶掉**（2026-08-12 19:17，無 Python traceback）。
  `start-panel.bat` 是看門狗迴圈，程式結束就自動重開。**sleep 用 `ping` 不用 `timeout`**
  （tool-manager 沒有 TTY，`timeout` 會失敗）。
- **真實下單全部集中在 `broker.py`**（2026-08-28 起）。面板本體與練習單一行都不碰真錢；
  要檢查「會不會亂送單」只需要讀那一個檔。**預設是 dry run** —— 要真的送單，
  必須有 `tools/shioaji/REAL_ORDERS_ON` 這個檔存在，而那個檔**只有 Benson 自己建**。
  改過 `broker.py` 一定要重跑 `test_broker.py`（離線、不連永豐、不送單，18 項）。
- ⛔ **永豐的 API 沒有停損單**（2026-08-28 查證）。`FuturesOrder` 只有
  LMT / MKT / MKP ＋ ROD / IOC / FOK，**沒有觸發價**（SDK 裡唯一的 `trigger_price`
  是報價掃描器的欄位）。所以：
  - **+100 停利可以掛在券商端等**（限價 ROD 平倉單），電腦關機也有效。
  - **−100 停損做不到券商端。** 以前這裡寫「正解是在券商端掛停損單」，**那是錯的**，
    透過這支 API 辦不到。
  Benson 知情後於 2026-08-28 決定「**停損也交給面板，風險我承擔**」，
  所以停損活在這台電腦的 Python 迴圈裡 —— 程式死掉／斷線／電腦睡著就沒有停損。
  能做的是讓他**知道**發生了（斷線警報），不是消除風險。**不要自作主張改掉這個決定。**
- **認期貨帳號不可以用「字串裡有沒有 F」**。要比 `AccountType.Future`。
  舊寫法會過純粹是因為 Stock / Intl 剛好都沒有 F，多一個帳號型別就會抓錯帳號下單。
- **不要看 `Account.signed`**：實測簽署完成後它仍然是 False，拿它當條件會變成
  明明能下單卻一直說沒簽。真的沒簽會在送單時報錯。
- ⛔⛔ **部位方向絕對不可以用「數量 > 0」推**（2026-09-01 在真實帳戶上出事）。
  舊寫法 `"long" if ("buy" in d or qty > 0) else "short"` —— `qty > 0` 對任何部位
  都成立 ⇒ **永遠回報做多**。Benson 做空的那一筆因此連環出錯：
  等不到「方向相符」的部位 → 誤判沒成交 → **沒掛停利**；畫面把空單顯示成多單；
  停損算在錯的一邊；**按平倉送出的是 Sell（該送 Buy），等於又加一口空單**。
  現在先認 `direction` 欄位、認不出來才看數量正負，**兩者都判不出來就回 `unknown`，絕不猜**。
- **`reconcile()` 要讓券商的資料蓋過本機那份**，不是只在本機空的時候才採用。
  舊版只有 `_state["position"] is None` 時才寫入 ⇒ **本機一旦記錯方向就永遠改不回來**，
  連 `close()` 先呼叫 reconcile 也救不了（它拿回來的還是那份錯的）。
  這個洞是補完第一版之後**測試才抓出來的** —— 修對稱性的 bug 一定要有一項測試
  直接斷言「送出去的是哪一邊」。
- **平倉前一定要跟券商重新確認方向**，不可以用記憶體裡那份。平倉是「反向下單」，
  方向錯就是部位加倍。對帳失敗或方向不明時**寧可拒絕**並叫他自己去大戶投平。
- ⛔ **「券商收到」≠「成交」，進場與平倉都一樣**（2026-09-01 兩次都栽在這）。
  範圍市價 IOC 當下沒撮到就整張不成交。`close()` 舊版把 `ok=True` 當成平掉了、
  直接清掉本機部位 ⇒ **券商還有部位、面板以為空手 ⇒ 停損不再監控，
  而且再按平倉會說「沒有部位」**。他最後是自己用別的工具平掉的。
  現在平倉要**確認部位真的消失**才算成功，沒撮到就重試（`CLOSE_TRIES`），
  全部失敗就保留本機部位並叫他自己去平 —— **絕不可以在沒平掉的時候清掉狀態**。
- **`cancel_order` 之前一定要先 `update_status`**，否則回
  `StatusCode: 400, Detail: Please run update_status`。撤不掉不可以默默吞掉：
  那張平倉限價單留在場上，成交後就是一個反向新倉。做法是照樣平倉（裸部位更危險），
  但把「請自己去刪那張停利單」講出來。
- ⛔ **`/api/real/close` 是「送出平倉」不是查詢。** 有真實部位時不要為了測試去打它 ——
  2026-09-01 我為了確認端點活著送了一個請求，直接平掉了他的部位。
  查詢用 `/api/state`，動作端點在有部位時一律不碰。
- **STATE 裡不可以放不能序列化的東西**。真實部位帶著永豐的 Trade 物件時，
  `json.dumps(STATE)` 會炸 ⇒ `/api/state` 回空字串 ⇒ **前端拿不到狀態、畫面整個凍住**
  （他手上有單卻看不到）。`snapshot()` 濾掉 `target_trade`，端點再加一層保險。
  ⚠️ 演練模式時那個欄位是普通 dict，序列化得了 —— **所以演練永遠測不出來**。
- **「有部位但方向不符」不可以當成「沒成交」**。那樣會清掉本機部位、畫面顯示空手，
  但券商那邊其實有東西 —— 而且後續的平倉會送錯邊。要停手並明講。
- **長按送單的那顆按鈕，長按期間不准重繪那張卡**（2026-09-01 踩到）。
  真實下單卡裡有現價，每次報價變動整塊 innerHTML 就被換掉、按住的按鈕當場被銷毀 ⇒
  畫面看起來像「按到一半自己取消」。**更危險的是計時器還握著那顆已經不在畫面上的按鈕，
  時間到照樣送單** —— 使用者以為取消了、單卻出去了。
  `holdingNow` 擋住重繪，計時器裡再加一道 `document.body.contains(el)` 保險。
- **`mouseleave` 要綁在按鈕本身，不可以用 document + capture**（同一次踩到）。
  綁在按鈕上時，游標在按鈕內部子元素之間移動不會觸發；綁 document 捕捉的話，
  離開任何一個子元素都算，游標稍微一動就取消。
  ⚠️ 測試時 `new MouseEvent('mouseleave',{bubbles:true})` 會造成假紅燈 ——
  真實的 mouseleave **不冒泡**，要用 `bubbles:false` 才測得準。
- ⛔ **進場也要有鎖，不是只有平倉**（2026-09-01 QC 退件）。券商回報部位有 1~2 秒延遲，
  他按完看畫面沒變就會再按一次。舊版平倉有 `closing` 擋著、**進場什麼都沒有** ——
  又是「對稱的東西只做了一邊」。兩張都出去＝ 2 口，而且兩張停利單都掛著、
  面板只記得後面那張 ⇒ **先掛的那張永遠撤不掉**。
  前端 `firing` ＋ 伺服器端 `ENTER_LOCK`（非阻塞，拿不到就回 409）兩層都要 ——
  前端擋不住重整、兩個視窗、前端出錯；`ThreadingHTTPServer` 會讓兩個請求真的同時跑。
  守衛：`test_enter_lock.py`（把面板自己的 Handler 架在空閒埠上，兩條執行緒同時 POST）。
- ⛔ **「查不到部位」≠「沒有部位」**（同一次 QC）。`_wait_fill()` 舊版把查詢失敗
  跟「查到了、是空的」混在一起 ⇒ 網路抖一下就清掉本機部位、跟他說「沒有成交」，
  但券商那邊其實有一口、**沒停利也沒停損**。現在 `broker_position()` 回 `"unknown"`
  要單獨計數，過半失敗就 raise：明講「不知道有沒有成交」、不掛停利、叫他自己去大戶投看。
  同一段的 `pos.get("entry")` 也要改成 `is not None` —— 成交價回報 0 不是「沒有成交價」。
- ⛔ **停利掛失敗不可以吞掉**（同一次 QC）。`enter()` 舊版 `place_target(tp)` 不看回傳值，
  掛不上去照樣回報一切正常 ⇒ 他以為賺的那邊有保護，其實只剩面板的停損（電腦關機就沒了）。
  現在 ok=True 但帶警告訊息（`warn`），前端一定要 alert；卡片上的「已掛在券商」
  改成讀 `has_target`，**不可以寫死**。
- ⛔ **對帳要獨立於 `can_enter()`**（同一次 QC）。舊版對帳只寫在 `can_enter()` 最後一步，
  前面任何一關先擋下來就不會執行 —— 而「當天已達 3 次上限」與「憑證沒啟用」都會先擋。
  於是：下完第 3 單、部位還開著 → 看門狗重啟（永豐 SDK 斷線會把行程帶掉，是常態）→
  因為已達上限**永遠不再對帳** → 部位撿不回來 ⇒ **停損完全不監控、畫面顯示空手、
  連斷線警報都不會響**（警報也要有部位才亮）。現在主迴圈每圈叫 `reconcile_tick()`，
  自己節流 `RECONCILE_EVERY`＝3 秒。
- ⛔ **`close()` 要互斥 ＋ 失敗後冷卻**（同一次 QC）。停損跑在主迴圈執行緒、
  他手按平倉跑在 HTTP 執行緒，兩邊會同時進來，各送一輪最多 `CLOSE_TRIES` 張。
  而且主迴圈每 0.25 秒就再判一次停損 —— 沒有 `CLOSE_COOLDOWN`（15 秒）的話，
  只要價格還在停損之外就會一直重送。拿不到鎖直接回「正在平倉中」，**不排隊**
  （排隊等於延後再送一輪）。
- **休市不算「報價中斷」**（同一次 QC）。13:45~15:00、夜盤收盤後、週末抱單本來就沒有報價，
  舊版一律跳紅色警報、一次響 75 分鐘以上 —— 狼來了喊多了，真的斷線那次他就不會理。
  `check_real_position()` 要收 `sess`，`closed` 直接清掉警報並返回。
- **長按送單只認左鍵**（同一次 QC）。Windows 的右鍵選單是**放開**才跳出來的，
  所以按住右鍵 650ms 一樣會送單。`mousedown` 開頭 `if(e.button!==0) return`。
- **面板要顯示自己載入的是哪一版**（同一次 QC）。看門狗會自動重啟，所以改到一半的
  `broker.py` 會在下一次崩潰重啟時被自動載入，**沒有任何提示**。
  `/api/state` 帶 `code`（broker.py / live_panel.py 的 mtime ＋ 行程啟動時間），
  卡片底下那行直接印出來 —— 不要再靠「比對檔案時間戳跟行程啟動時間」用推的。
- ⛔ **「演練不准清掉本機部位」不適用於從券商撿回來的部位**（2026-09-01 12:43 在他機器上實測）。
  面板在真實模式時撿回一口多單（`recovered=True`），之後他刪掉 `REAL_ORDERS_ON` 改回演練、
  並自己用別的工具平掉那口單 ⇒ 券商已經空手，面板卻因為「演練不准清」永遠掛著那口鬼部位：
  `can_enter` 一直回「已經有部位了，先平倉才能再進場」⇒ **他從此不能再進場**，
  浮動損益也是拿現價去對一個不存在的部位算的。
  要保護的只有**演練自己造出來的**部位（`enter()` 寫 `recovered=False`）；
  `recovered=True` 的本來就是真的，券商說沒了就要清掉。`reconcile_tick()` 同理，
  演練模式平常不問券商，但有 recovered 部位時要照問。

- ⛔ **測試裡 broker 每一個會寫檔的常數都要導到暫存區，漏一個就污染他的真實紀錄**
  （2026-09-01 踩到）。`test_broker.py` 一直有把 `ORDER_DIR` 導走，加了 `real_trades/`
  之後卻只在成績單那一節才改 `TRADE_DIR` ⇒ **前面那些 `close()` 的測項照樣寫進真的
  `real_trades/`**，他的成績單多了 6 筆假交易（做空 46978、理由印著 `test`）。
  跑了兩輪、測試全綠，是 **lab-ux 在量畫面時發現筆數不對才抓到的**。
  現在檔頭有 `REAL_DIRS` 一次導走，收尾還有一節斷言「全程沒有指回真的資料夾」。
  新增任何會寫檔的常數時，那兩個地方都要跟著加。

- ⛔ **練習那邊一定有的欄位，真實那邊不一定有**（2026-09-02 當場踩到）。把真實交易畫上
  K 線圖時直接沿用練習的算式 `Math.min(lo, t.entry, t.exit)` —— 真實單的 `exit` 可能是
  `null`（問不到成交價時留白），`Math.min(x, null)` 會把 null 當成 **0** ⇒
  **價格軸整個掉到 0、K 棒被壓成畫面頂端一條線**。測試沒抓到，是**截圖才看見**的。
  凡是把真實資料餵進練習那套算式，先問一次「這個欄位在真實那邊會不會是 null」。
  守衛：`tabs-visual.mjs` 驗 `window.AXIS` 的上下緣沒有掉出 K 棒範圍。
  ⚠️ 那條斷言的第一版是「掃 svg 裡所有像價格的數字」，**會把成交量軸的數字刮進來**
  （量是 1250 之類的四位數）⇒ 誤判成軸被拉爆。要驗就驗程式算出來的那個值本身。
- **真實交易也能寫心得**（2026-09-02）。跟練習同一套 UI，但存進 `real_trades/`、
  **不上傳**（走 `/api/note` 的 `kind:"real"`，刻意不放 `/api/real/*` —— 那個前綴的意思是
  「會送出委託單」，我們有一條「有部位時不碰 `/api/real/*`」的鐵律，寫心得不該混進去）。
  ⚠️ 分區字母用**大寫 R**：小寫 r 已經被回顧分頁佔走，撞了會讓 `nEditing()` 分不出
  是誰在編輯，兩邊的輸入框互相打架。編輯中同樣要擋重繪（中文輸入法會掉字）。
- ⛔⛔ **今天的 K 棒拿不到時，圖會整片是昨晚、卻掛著今天的日期**（2026-09-02 他回報
  「加載完了但 K 圖還是不是最新的」）。實測面板：**109 根、最後一根停在 23:45、全部都是
  前一晚**。根因是 `session_frame()` 只要 `own`（今天那份）是空的就靜靜地只回夜盤。
  現在 `partial` 從布林改成**種類字串**：`'night'`（夜盤沒到）／`'today'`（今天的整個拿不到）
  ／`''`（都齊），兩種的文案完全不同 —— 前者說「夜盤載入中」，後者說「今天的 K 棒還沒拿到，
  圖上是昨晚的夜盤」。⚠️ **只有 `'night'` 才不准猜夜盤**；`'today'` 的時候夜盤反而要畫
  （那是手上唯一真的資料）—— 第一版沒分，把「只有夜盤」那個測項打紅了。
  守衛：`test_session_frame.py` ⑤⑥（變異測試驗過拿掉會紅）。
  ⚠️ **⑤ 曾經是一條「每天都紅」的守衛**（2026-09-07 lab-qa 開單、當天修掉）：
  那一節寫死 `D = 2026-09-02`，而 `partial='today'` 的條件是 `d == date.today()`
  ⇒ **只有 2026-09-02 當天會綠**。用 `git worktree` 拉 HEAD 重跑確認 HEAD 也一樣紅，
  不是哪一輪改壞的。修法是**在那一節（也只有那一節）假造 `date.today()` 與
  `datetime.now()`**（`fake_today()`，用完 `finally` 還原並斷言還原了），
  ⛔ **只改測試、不動 `session_frame()`**；另配一個對照組（同一個假時鐘、今天的 K 棒補回去
  ⇒ 不准再喊 `today`），證明它不是被假時鐘設成恆真。
  **一條永遠紅的守衛跟永遠綠的一樣沒用** —— 大家會學會忽略它。
- ⛔ **啟動時的狀態訊息不可以寫死**（同一天）。舊版一律是「已連線，等待 08:45 開盤…」，
  於是他 10:14 盤中重開面板時，畫面同時出現「盤中卻收不到報價」與「等待 08:45 開盤」，
  兩句互相打臉。盤中剛啟動的真實狀況是「還沒收到第一筆 tick」。改用 `boot_msg()` 看時鐘決定。
- ⛔ **pywebview 的 width/height/x/y 是「邏輯像素」，它會自己乘上縮放**
  （`winforms.py`：`Size(int(initial_width * scale))`、`Point(int(initial_x * scale))`，
  `scale = GetDpiForWindow()/96`）。所以**絕對不可以**先呼叫 `SetProcessDPIAware()`
  再餵它實體像素 —— 會被再乘一次。2026-09-02 我就這樣弄反：算出 1520×980 @ (200,20)（實體），
  它乘 1.25 變成 1900×1225，**比螢幕還大** ⇒ Windows 推到左上角、右邊底部切掉，
  跟修之前一模一樣。正解：直接用系統回報的工作區（行程沒有 DPI 感知時本來就是邏輯像素），
  夾住尺寸（留 6%/8% 的邊）再置中。實測邏輯 1443×750 @ (46,33) → 實體 1803×937 @ (57,41)。
- ⛔ **桌面 App 的視窗尺寸要夾住，而且要在 pywebview 之前先設 DPI 感知**（2026-09-02）。
  他回報「每次開的位置都很奇怪、不在正中央」。根因有兩層：① 寫死 1520×980，
  而他的可用高度只有 1040 —— **視窗比螢幕還大**，Windows 只好隨便擺、右邊底部被切掉；
  ② pywebview 會在建立視窗時自己呼叫 `SetProcessDPIAware()`，之後座標是**實體像素**；
  我們若在那之前量，系統回的是**虛擬化後的邏輯像素**（他的機器縮放 125%：量到 1536×864、
  實際 1920×1080），差 1.25 倍，中心會整個偏掉。
  `window_box()` 先自己呼叫一次 `SetProcessDPIAware()` 再量工作區（扣掉工作列），
  夾住尺寸並置中；算失敗就退回系統預設，不要因為擺位失敗就開不了視窗。
- **真實成績（勝率）不受「真實下單」開關管**（2026-09-02 他要求）。`REAL_ON` 管的是
  「會不會送單」，看自己過去的成績跟送不送單無關；而且那個開關**刻意不記憶**
  （重啟後一定是關的），綁在一起等於每天早上都看不到昨天的成績。
  ⚠️ 但**下單按鈕仍然受開關管**——保險蓋要留著（探針有守：開關關著時 `[data-rdir]` 是 0 顆）。
  勝敗定義要跟練習那邊同一套（**點數 > 0 才算勝，0 算敗**），否則同一批交易會算出兩個勝率；
  勝率只算得出點數的那幾筆，算不出的要**明講排除幾筆**，不可以默默不算。
- ⛔ **真實區要列得出過去的日子，不是只有今天**（2026-09-03 他問「昨天的交易紀錄都不見了」）。
  檔案好好的在 `real_trades/2026-09-02.jsonl`，心得也在 —— **是畫面只列今天**：
  清單標題就寫著「今天的真實交易」，`trades_today()` 只讀 `{today}.jsonl`。
  一過午夜，昨天那幾筆就從畫面上消失，只剩併進勝率裡的一個數字；
  回顧分頁又只讀 `practice_trades/`（練習），所以**過去的真單全站無處可看**。
  **留得住不等於看得到。**
  ⚠️ 第一版的修法（`realPast()`：另開一段「過去的真實交易」、用真實區自己的
     `.n-row` 五欄再加日期標頭）**當天就被他退件**，理由是下一條。已整段移除，
     `.t-past` 與 `.n-dayh` 都不存在了。
- ⛔⛔ **「跟練習一樣」指的是形式，不是「也看得到」**（2026-09-03 他退件：
  「真實的交易紀錄要跟練習的交易紀錄的形式長的一樣，我不是說過了嗎」）。
  第一版只是把過去的紀錄補出來，樣式沿用真實區自己的五欄列 —— 東西看得到了，
  但**不長得像交易紀錄**。lab-ux 是去比對**第三個介面**才確定的：他手機 App 的紀錄
  （`js/app.js`）跟面板「練習成績」底下那份用的是同一套 `.trade` 卡片 ⇒
  **在他所有看得到紀錄的地方，紀錄都是一張卡片，唯一的例外就是真實區。**
  這是語意層級的問題，換顏色、換間距都解不掉。
  **通則：他說「像 X 一樣」時，先去找他在第三個地方看同類東西的樣子** ——
  兩個地方對比只看得出差異，三個地方才看得出哪一個是異類。
  現在真實區的結構逐項對齊練習（A 版，他從三個 demo 中親自挑的）：
  今天的五欄清單在上 → `.n-sep` → 「真實成績」（`.seg` ＋ 勝率 ＋ 勝敗條 ＋
  跨日 `.trade` 卡片清單）。今天那幾筆會上下各出現一次 —— **練習那邊本來就這樣**。
  - `realCard()` 欄位對照練習的 `row()`；`exit`／`points` 可能是 `null` → 印「—」、
    用 `.tr-res.na`，**卡片不加 `.win`/`.loss`**（算不出點數就不猜輸贏）。
    算得出來的照練習掛紅賺綠賠。
  - `realWindows()`：近7／近10／近30／全部，**按筆數去重，只剩一個就整條不畫**。
    真實筆數少，四顆常常是同一批資料 —— 按了畫面完全不動比沒有這排按鈕更糟。
  - `realSorted()`：依 `date + entry_time` 新到舊。**不要依賴 `trades_all` 的既有順序**
    （它現在剛好是新到舊，但那是 `trades_history()` 的實作細節不是保證）。
  - `realScore()` 抽成獨立節點 `#realscore`（只放跟窗口有關的那幾塊，**裡面不准有輸入框**）：
    按分段時就地更新、不碰 `.list` ⇒ 正在寫心得時按分段也會即時生效、字不會掉。
  - **卡片心得用分區字母 `S`、五欄清單用 `R`**（跟練習的 `s`/`t` 對稱）。同一筆今天的交易
    會同時出現在兩份清單，**同 key 會展開兩個 `id="tnote"`**，`getElementById` 只拿第一個
    ⇒ 在另一個打的字存不進去。重繪守衛因此要 `!nEditing('R') && !nEditing('S')`。
  - **沒有「下載紀錄」鈕**（他的決定）：練習那顆寫著「可匯入 App」，而 App 會同步到這個
    公開 repo ⇒ 真實區放一顆長一樣、格式也一樣的，他哪天順手匯進去就等於上傳了，
    而且沒有任何一道防線會擋。
  - 平倉理由的標籤有 **4 字硬上限**（`RWHY` 是 `.n-row` 與卡片的單一來源）：
    卡片的 `.tr-px` 只有 157.6px，6 字 ＝ 161px 會折行、那張卡 65→80px。
    所以 `closed_elsewhere` 是「別處平的」不是「不是面板平的」。⑧b6 在守。
  ⚠️ 切「今天／過去」一定要用後端給的 `snapshot()["today"]`，**不可以用瀏覽器的
     `new Date()`** —— 跨午夜那一刻兩邊會不同一天，同一筆交易會在兩個清單各出現一次。
  ⚠️ 真實區有兩份清單，各帶標記 class（`.t-today` ＋ 成績區的 `.list`）：
     探針原本用 `.n-zone.z-real .n-row` 數「今天有幾筆」，加了第二份就當場紅了，
     **不可以靠「第幾個清單」認人**。
  守衛：`test_broker.py`「過去的成績單」一節（跨日讀取、補心得不可以弄丟同一天的其他筆
  ／不可以動到別天／認不得就拒絕）＋ `hold-to-fire.mjs` ⑧b／⑧b6／⑧b7。
- ⛔ **這一列的欄寬是靠容器寬度算出來的 —— 搬區塊等於改欄寬**（2026-09-03，
  他早上看到的成交價是「4634…」而不是完整的進出場價，已經這樣兩天）。
  `.n-row` 的價格欄是唯一的 `1fr`，容器每窄一點全部從它身上扣。
  兩份清單一度被包進 `.n-bd`（padding 左右合計 38px），而練習的 `#simtr` 直接掛在
  `.n-zone` 底下沒有那一層 ⇒ `.n-row` 310px vs 練習 348px、價格欄 **38px vs 76px**、
  **12/12 全截**。正解是把清單掛在 `.n-bd` 外面，**不是覆寫寬度或改 grid 欄寬**
  （那只是把問題推到別欄，下次再搬一次又會壞）。
  ⚠️ 舊版曾經用 `text-overflow:ellipsis`「修」過這件事 —— 那只是把「折行讓那一列變高」
     換成「價格看不到」，**兩者都不對**，而且**筆數少的時候看起來完全正常**。
  ⚠️ 目前餘裕只剩 3.4px（最寬的字串 72.58px vs 欄寬 76px）：**指數上六位數、
     或字級／字距一動就會再被截**。⑧c 會擋住，但擋住之後只能靠加寬欄位。
  **搬完任何區塊都要逐一量價格欄的 `scrollWidth`，不准只驗前三顆。**
- **真實交易的成績單在 `real_trades/`，跟 `real_orders/` 是兩回事**（2026-09-01 加）。
  `real_orders/` 是每一張委託單的原始 log（給程式與除錯看）；`real_trades/` 是一趟來回
  （幾點進、幾點出、賺賠幾點），面板的真實下單卡直接列出來。**兩個都不上傳**——
  練習成績那一區會同步到 `data/practice.json`、推上公開 repo、再拉到手機，
  所以真單**絕對不可以**混進那一區（Benson 的決定）。

- ⛔⛔ **改完任何檔案、commit 之前一定要跑 `py tools/probe/leak-scan.py`**（2026-09-03 加）。
  它比對 `real_trades/*.jsonl` 的每一個欄位，掃 **`git ls-files` ∪ 未追蹤但沒被 gitignore
  的檔**，命中就非零離開。**「照抄」不是只有價格** —— 一筆交易的
  **進出場時間**（幾點進場）、**賺賠點數**、**心得原文**，任何一項單獨拿出來
  都是他的真實紀錄，跟成交價一樣不可以進公開 repo。
  背景：2026-09-03 一天之內同一條規則被撞開**六次**（PM 兩次、dev 兩次、QA 抓到兩次），
  每一次的想法都是「我把價格換掉就安全了」，於是治具與測試裡留著他真實的
  進場時間與賺賠點數。**靠人盯已經證明會漏**，所以做成會擋的檢查。
  ⚠️ **只掃 `git diff` 不夠**：`test_broker.py` 就是因為「那一輪沒改到它」而躲過一次掃描。
  ⚠️ **掃描範圍一定要含「還沒 git add 的檔」，包括掃描器自己**：第一版只掃 `git ls-files`，
     而它自己還沒 add ⇒ 它的註解裡舉例用了兩個他真實的成交價，**自己卻報乾淨**。
     現在 `main()` 會斷言「掃描清單裡有這支自己」，沒有就當尺壞了、非零離開。
  ⚠️ 這支自己會先做「尺的自證」（8 條，含 4 條負控組）。**它自承的漏洞寫在檔頭**，
     最要緊的三條：① **截斷成 `HH:MM` 的時間完全不比對**（那正是畫面與 `/api/note`
     用的形式；納入會命中 6 萬多筆全是雜訊，只能靠人警覺）；② **單獨只洩漏點數欄
     抓不到**（`100` 是固定的 ±100 規則值，單獨比對 318 處雜訊）；③ **`.csv` 整類不掃**。
  ⚠️ **命中不一定是抄的，也可能是巧合**（合成 K 棒示範資料剛好撞到同一個五位數，
     `review-demo.html` 就發生過兩次）。處置一律是**把那個數字改掉，不要加豁免名單** ——
     看的人分不出巧合與外洩，那就不該留在公開 repo 裡。
  ⚠️ 已知的真實紀錄**歷史上已經進過 repo**（`fe_harness.py` 那筆沒有損益數字的手動平倉）——
     Benson 2026-09-03 裁示**換掉現在的就好、不改寫 git 歷史**。
- ⛔ **市價單的成交價只能事後問**。送出時價格欄是 0（MKP），所以「賺賠多少」不在委託 log 裡。
  現在兩條路：平倉成功後讀那張單的 `status.deals`（加權平均）；補不到就跟券商要
  `list_profit_loss`（今天的已實現損益，**那才是真相**，含 entry_price/cover_price/pnl，
  快取 60 秒）。**問不到就留白，絕對不可以拿現價或參考價冒充**——
  留白看得出來是缺，編的數字看不出來。
- ⛔ **停利在券商成交時，`close()` 根本不會被呼叫**。那張限價單掛在券商、面板永遠不送它，
  所以部位是在**對帳時「自己不見的」**。不在 `reconcile()` 那個分支補記一筆，
  這趟來回就完全不會出現在成績單上（賺錢的那些全部漏掉）。
  分不出是停利成交還是他自己在別的工具平掉：問得到停利單的成交價就記 `tp`，
  問不到就記 `closed_elsewhere`，不猜。

- ⛔ **關視窗再開 ≠ 重開面板**（2026-09-01 白重開一次才發現）。`panel_app.pyw` 看到
  伺服器活著就直接接上去（用意是不必每次開窗都重連永豐、等一分鐘），所以改過程式之後
  關視窗再點一次，**接到的還是那個舊伺服器、新程式永遠載不進來，畫面上完全看不出來**。
  現在三道：面板自己回報 `code.stale`（硬碟上的檔案比行程新）／`panel_app.pyw` 接上去
  之前先看 stale，是就把舊的收掉再開新的／前端在卡片上直接寫「面板跑的不是最新的程式」。
  手動要換版就跑 `restart-panel.py`（會關乾淨、重開、並把版本指紋印出來）。
  ⚠️ `code_stamp()` 讀的是**當下的檔案 mtime**，不是「載入時的版本」——
  所以判斷過期一定是拿 mtime 跟**行程啟動時間**比，不能拿它當「我載入了哪一版」。

- ⛔ **治具的假狀態一定要會動，否則驗不出任何「重繪」類的 bug**（2026-09-01 lab-qa 用變異
  測試證明）。`fe_harness.py` 的現價本來是固定值 ⇒ `realBody()` 每輪產出同一個字串、
  `setEl` 判定「沒變」就不碰 DOM ⇒ **「重繪把按住的按鈕換掉」這個故障在治具上根本重現不了**。
  把 `if(!holdingNow) paintRight(...)` 那道守衛整個刪掉，探針照樣 35/35 全綠。
  現價與浮動點數改成跟著時間跳之後，同一份壞掉的程式立刻 7 項紅。
  ⚠️ 連帶：探針不可以斷言「浮動點數 = -12」這種固定值，要驗格式與**正負號**。
- ⛔ **「按到一半按鈕還在嗎」不可以用 `querySelector` 重查**（同一次抓到）。
  按鈕被換掉之後查到的是新生出來的那一顆，`contains()` 當然為真 —— **結構上永遠成立**。
  正確做法：**按下去之前**先掛 `mousedown` 監聽把那一顆節點存起來，之後驗同一顆還在不在，
  並且加一項「真的抓到節點了」當尺的自證。
- ⛔ **`REAL_ON` 只管「露不露出下單按鈕」，不管「看不看得到部位」**（2026-09-01 lab-qa 退件）。
  舊寫法在開關關著時整個 body 都不畫 ⇒ **站在寫著「真實」的分頁反而看不到自己的真實部位、
  也沒有平倉鈕**，而唯一的補償訊號（頁籤上的浮動點數）又只在「不在真實頁」時才掛 ⇒
  **站在真實頁＝知道得最少**。而看門狗重啟是常態、`REAL_ON` 又刻意不記憶 ⇒ 重啟後一定是關的，
  偏偏 `reconcile()` 會把券商那口單撿回來，這個組合遲早遇到。
  ⚠️ `paintRight()` 的骨架識別鍵 `zk` 因此**必須帶「有沒有部位」**，否則部位出現時骨架不重建。
- **探針的「console 零錯誤」要放在跑完之後驗**，不是只驗第一節。只印不 `FAIL++` 的話，
  掛排程或只看「總結」的人會被安靜放行。

- **長按送單有真滑鼠探針**（`tools/probe/hold-to-fire.mjs`，16 項）。前端這一段的 bug
  全藏在事件細節裡，`dispatchEvent` 造假事件測不出來，所以用 CDP 的
  `Input.dispatchMouseEvent`（跟 `press-scan.mjs` 同一招）。要先起治具
  `tools/probe/fe_harness.py`（8771，假狀態、不連永豐、POST 只記錄不送單）。
  守：右鍵長按不准送／沒到門檻不准送／到門檻送一張／長按期間卡片重繪按鈕不能被換掉／
  送出中連按第二下不准變第二張／停利沒掛上卡片要照實說＋要跳警告／版本指紋有印出來。
  ⚠️ 這支自己踩過三個會**變成假綠燈**的坑，改它之前先看檔頭：
  ① headless 預設 800x600，下單卡被擠到 y=2306 ⇒ 座標在畫面外、**打不到任何元素**，
     「沒送出單」那幾項全部白過 —— 現在 `--window-size=1400,1000` ＋ `box()` 自己驗
     座標在畫面內、而且那一點上面真的是目標按鈕。
  ② 面板送單後會 `alert()`，**alert 會把頁面 JS 卡死** ⇒ 之後每個 `Runtime.evaluate`
     都不回，探針無聲掛住 —— 一定要接 `Page.javascriptDialogOpening`。
  ③ 卡片裡的分隔用的是**全形空格**，正規表示式寫 `程式 .* 啟動 ` 永遠對不上。

- **停損觸發邏輯有離線測試**（`test_stop_trigger.py`，18 項）。永豐沒有停損單、
  Benson 選擇交給面板，所以 `check_real_position()` 那幾行就是他的停損，判斷錯就是真錢。
  守的性質：到價才平／**停利那一邊面板不可以出手**（那張限價單在券商，重複送會變反向新倉）／
  **報價不新鮮時絕不拿舊價判停損**、但要記下「從何時開始瞎了」／報價一回來就補平。
  這支不連永豐（`broker.close` 換成假的），改停損邏輯就要重跑。
- **模擬帳戶跑通整條路，做多做空各一輪**（2026-09-01，`test_broker_sim.py`）。
  **這是唯一驗證過「真的送出去」那一段的方式**，改過 `broker.py` 的下單流程就要重跑。
  ⚠️ 那支會真的呼叫 `place_order`，只是連的是模擬帳戶。
  實測數字（做空那輪就是 09-01 出事的那條路）：
  | | 做多 | 做空 |
  |---|---|---|
  | 成交 | 46999 | 46996 |
  | 停利 | Sell 47099（+100） | **Buy 46896（−100）** |
  | 平倉 | Sell | **Buy**（舊版送 Sell ⇒ 再加一口空單） |
  ⛔ **只測一個方向等於沒測。** 09-01 之前只跑做多，跑了兩輪全綠，
  卻完全沒抓到「方向永遠回報做多」這個會賠錢的 bug。
- **停利停損一定要用「實際成交價」算，不是送單當下的參考價**（模擬實測抓到）。
  範圍市價本來就有價差：兩輪分別差 2 點與 **6 點**（參考 46964 → 成交 46970）。
  `enter()` 送出後會等券商回報成交（最多 `FILL_WAIT` 秒）再算停利。
- **IOC 沒成交就絕對不可以掛停利**（同上）。MKP + IOC 撮不到就是整張不成交，
  舊寫法照樣把 Cover 單掛出去 ⇒ **那張留在場上，成交後就變成一個反向的新部位**：
  明明沒進場卻多一個空單。現在確認有部位才掛，沒成交就記 `entry_nofill` 並清乾淨。
  **這個只有真的送出去才會暴露，演練模式永遠測不到。**
- **演練模式必須跟正式模式行為一致，只差「不送出去」**（2026-09-01 踩到）。
  `reconcile()` 同時扛三件事：券商連不上就不送、券商已有部位就不送、券商沒部位就清掉本機部位。
  演練時只有第三件要跳過（券商本來就沒部位，不跳過的話長按送出的部位一秒就被抹掉、
  持倉畫面根本演練不到）。第一版圖省事整個 `return` 掉，**把前兩道防線也一起關了** ——
  等於他在一個守衛比較鬆的模式裡練，真的開起來才發現行為不一樣。
  `test_broker.py` 那三項就是在守這件事，**是 Benson 跑測試抓出來的**。
- 台股慣例：**紅色=漲/賺、綠色=跌/賠**（跟美股相反）。PWA 與面板都要一致。
- **「可以標極端，不可以標方向」**（2026-08-25 視覺升級時裁定）。位階／量能的量尺
  超出常態時會變金色 —— 那是在講「這個已經發生的數字很極端」，不指方向、不建議動作，
  性質跟 K 線上一根特別長的棒子一樣，所以不算違反上面那條紅線。
  界線是：**不准出現任何暗示「所以會漲／會跌／該進場」的顏色或文字。**
  詳見 `tools/shioaji/UI-REDESIGN-SPEC.md` §15。
- **心得（note）是事後補寫的**，跟手機 App 同一個欄位。面板改紀錄一律只動 `note`，
  並用（日期＋進場時間＋進場價）認人 —— 用陣列位序會被「撤銷最後一筆」弄歪。
  編輯中的那一區要停掉自動重繪（中文輸入法打到一半整個 textarea 被換掉，字會不見），
  但「展開輸入框」本身也是重繪，要能繞過那道保護。
- **測練習功能不要拿真的紀錄下去試** —— 寫 note 會觸發 `sync_to_cloud()`，
  假資料會被 commit + push 進公開 repo 的歷史（踩過，2026-08-25）。要試就複製一份到別的資料夾。

## 桌面 App（panel_app.pyw）

雙擊桌面／開始功能表的「早盤儀表板」→ `pythonw.exe panel_app.pyw`：
先確認伺服器活著（沒有就開起來並看門狗顧著），再**自己開一個視窗**（pywebview，
底層是 Windows 11 內建的 WebView2）；視窗關掉就整組收乾淨。

- **視窗不能交給 Edge 開**（2026-08-28 換掉）。用 `--app` / `--app-id` 時視窗的擁有者是
  Edge，Windows 就把它算在 Edge 頭上 ⇒ **工作列顯示 Edge 的圖示，安裝成 PWA 也一樣**。
  現在視窗是 `pythonw` 這個行程的，開視窗前先
  `SetCurrentProcessExplicitAppUserModelID("Benson.MorningPanel")` ＋ 帶 `icon=panel.ico`，
  圖示與工作列就都是我們的（實測視窗擁有者從 msedge 變成 pythonw）。
- 依賴：`pywebview`（＋`pythonnet`），裝在 repo 的 .venv 裡。WebView2 執行階段 Win11 內建。

- **捷徑會被 Edge 蓋掉**（2026-08-28 踩到）。把面板安裝成 PWA 時，Edge 會在桌面建一個
  **同名**的捷徑，直接覆蓋我們那個 —— 之後點下去只會開視窗、**不會啟動伺服器**，
  畫面就是「127.0.0.1 拒絕連線」。安裝完要把捷徑重寫回指向 `panel_app.pyw`。
- 設定檔（WebView2 的 storage）在 `%LOCALAPPDATA%\MorningPanelApp`，跟他自己的 Edge 分開。

### 走過但行不通的路（別再試一次）

用 Edge 開視窗的那一版踩了一整串，全部作廢，留著當紀錄：
- `--app-id` 啟動的行程 **1 秒就自己結束**（交棒給既有的 Edge），拿它當「視窗關了」會一開就誤判。
- 改看「多久沒人來要 `/api/state`」也不行：視窗**縮到最小或切到背景時 Edge 會把頁面計時器降頻**，
  看起來像沒人在看，伺服器會被誤殺（實測 24 秒沒輪詢，視窗其實還開著）。
  `/api/idle` 這個端點還留著，當作除錯用。
- `--app-id` 與 Edge 自己捷徑那組 `msedge_proxy`（`--profile-directory=Default --app-id=…
  --app-url=…`）在專屬設定檔底下**都開不出視窗**（沒有任何視窗、`/api/idle` 一直是 null）。
- app id 不要自己算：照 Chromium 那套 SHA-256 推出來是 `kkbjodnh…`、Edge 實際用 `gflghkeo…`。
- **裝成 PWA 也救不了圖示** —— 裝好了工作列還是 Edge 的圖示（Benson 親自確認）。

## 檔案配置

```
tools/shioaji/
  live_panel.py          面板本體（單一檔，HTML/CSS/JS inline 在 PAGE 常數）
  start-panel.bat        看門狗啟動（ASCII + CRLF）
  _config.py             從 .env 讀 API Key（.env 已 gitignore）
  shioaji_export.py      抓歷史 1 分 K
  build_intraday.py      建盤中特徵矩陣（研究用）
  walkforward.py         走查驗證（研究用）
  barrier_walkforward.py 用真正的 ±100 規則驗證（研究用）
  model_shootout.py      多模型對照（研究用）
  review_my_trades.py    用 (日期,時間) 把他的交易對回當時盤面
  tick_writer.py         逐筆報價落地（記憶體佇列＋獨立寫檔執行緒；on_tick 不碰磁碟）
  tick_recorder.py       輪詢版取樣工具（讀 /api/state，不用重啟面板就能錄）
  tick_logs/             逐筆紀錄（gitignore）YYYY-MM-DD.jsonl ＝逐筆、
                         YYYY-MM-DD-polled.jsonl ＝取樣，兩種 schema 不可混
  practice_trades/       模擬練習紀錄（gitignore，會同步到 data/practice.json）
  replay_log/            重播練習紀錄（gitignore）★ 絕不可與 practice_trades 混用
  TICK-TAB-SPEC.md       【細節】分頁（逐筆早盤圖）的設計規格
data/practice.json       練習紀錄的雲端同步檔（面板自動 push，手機 App 自動拉）
tools/probe/
  tick_synth.py          合成逐筆資料產生器（治具與探針共用；⛔ 價格一律 12000 附近）
  tick-backend.py        【細節】後端探針（離線，58 項，唯讀他的 tick_logs）
  tick-tab.mjs           【細節】前端探針（真瀏覽器＋真滑鼠，108 項；要先起 fe_harness.py）
```

**練習紀錄與重播紀錄必須分開**：`practice_trades/` 是他真實的練習成績、會同步到手機；
重播是「回頭重做一次判斷」，混進去會污染勝率統計。

## 動效基調（沉穩）＋ 開場（白起） — 2026-08-28 上線，v20（v21 修狀態列淺色帶）

範本正本在 `../app-template/motion/`（跨 App 共用，已上線、已 QA 放行）。
這支是**第二支**套用的 App，第一支是 `movie-library`（好雷嗎? v1.6.3），
做法逐項照它 —— 那一版是老闆說「完美」的參考實作，**要改先去看它的 CLAUDE.md 第 30～37 條**。

### 檔案

```
css/motion.css       動效 token（15 個，數值逐字照範本）＋ 綁**這支 App class 名稱**的規則
css/splash.css       範本複製品（逐位元組相同，不可以在這裡改）
js/splash.js         範本複製品（同上）；<body> 尾端、排在 app.js 之前
js/splash-boot.js    範本複製品；**不被 <script src> 載入**，已逐字 inline 進 index.html
index.html           關鍵路徑 CSS ＋ SPLASH_CONFIG ＋ inline 的 splash-boot ＋ 非阻塞 CSS ＋ 落地色票
tools/check-splash.js       靜態守衛（沒有 npm test，這支就是這一層的測試）
tools/probe/*.mjs           真瀏覽器探針（first-frame / press-scan / paths / flows / status-band / safe-area）
```

### ⛔ 絕對不要整包套範本的 `motion.css`

範本那份帶著自己的色票，而它的 **`--bg` / `--surface` / `--surface-2` / `--line`
跟 `css/style.css` 四個變數全部撞名** —— `<link>` 進來會把交易日誌的深色配色
（連同 `@media (prefers-color-scheme:light)` 那一組覆寫）整個蓋掉。
所以 `css/motion.css` 是**新寫的**：只有 15 個動效 token ＋ 綁自家 class 的規則，
**一個色碼都沒有**（`tools/check-splash.js` 有斷言在守；高亮用的金色是 `var(--gold)`，
所以淺色模式自動跟著換）。要同步範本的新版動效時，一樣只搬 token 與規則。

### 動效只做這五項，不要發散

1. **按下回饋**（最有感）——全 App 每一個可點元素，不是挑幾個。
   清單是 `tools/probe/press-scan.mjs` 用真滑鼠事件**全掃描**出來的，不是列白名單。
   ⚠️ 鑰匙圈藥丸 `.kr-chip` **刻意不寫在 motion.css**：模組自己就帶
   `.kr-chip.kr-chip:active{transform:scale(.97)}`，而且樣式是 `init()` 時注入 `<head>` 尾端，
   權重與順序都贏我們。在這裡再寫一條只會變成同一條規則活在兩個地方，還蓋不過去。
2. **清單進場錯開** —— 換模式時整批播；**展開某個月份時只有那個月的列播**
   （`renderList(quietExcept)` 決定要不要掛 `.anim`）。不做這件事的話，點一下手風琴
   整張清單會跟著閃一次，看起來像壞掉。
3. **表單面板開關** —— 離場用**獨立的** `tl-sheet-out` / `tl-fade-out`，
   不是把進場反著跑（`animation-name` 沒變的話瀏覽器不保證重播）。
   **流程不掛 `animationend`**：`closeSheet()` 用計時器把 `.closing` 拿掉；
   就算計時器沒跑到，`.closing` 的終態跟關閉後的靜態值一模一樣 ⇒
   失敗模式是「看不出來」，不是「面板永遠關不掉」。
4. **剛存的那一筆高亮**（這支才有的狀態）—— 存完／改完掛 `.fresh`，
   進場 ＋ 金色光暈；**2 秒後不再標記**，之後的重繪（背景同步拉回紀錄、換月份）不會再閃一次。
5. **開場**（白起變體）。

⛔ **刻意沒做的**：
- **不做骨架屏**。資料在 localStorage、開啟是即時的，**沒有東西要等**，加一個假的骨架屏是騙人。
- **不做頁面 push/pop**。這支只有一頁。
- 不因為動效新增任何按鈕或連結。

### 進場動畫一律 `backwards`

`both` / `forwards` 會永久保留最終 transform，優先度高於一般宣告 ⇒
`:active{transform:scale()}` 從此失效、按鈕變成死的，**而且純功能測試抓不到**。
唯一的例外是離場（`.closing` 的 `*-out` 用 `both`，那本來就要停在終點）。

### 開場：白起（`<html data-splash-intro="light">`）

iPhone 從主畫面開 PWA 時，iOS 會先播自己的啟動畫面（`background_color` ＋ icon），
約 0.5s 開始**平滑淡出**成接近全白（`#ebebeb`），我們的頁面才畫第一幀。
白起的做法是**接住那片白**：第一幀就是 `#ebebeb`，700ms 內平順沉成 `#0F1218`，
金色「誌」在過程中浮出來，名字等底色沉完才升起。畫面上沒有硬切點可以「閃」。

- **落地值**（`index.html` 最後一塊 `<style>`）：
  `--splash-bg:#0F1218`（＝ `manifest.background_color` ＝ 深色 `theme-color` ＝ 深色 `--bg`）／
  `--splash-ink:#E7E9ED`（＝`--text`）／`--splash-accent:#E3A951`（＝`--gold`）／
  glyph `誌`／name `微台指交易日誌`／tagline `一天一筆，記錄與檢討自己`（印記風格不畫標語，只進快取）。
- **`manifest.webmanifest` 一個字都沒改**（`background_color` / `theme_color` 仍是 `#0F1218`）。
  ⚠️ 另一條路（C1：連 manifest 一起改白）代價是**他必須把 App 從主畫面移除重新加一次**，
  而且 localStorage 看起來會像被清掉。`movie-library` 那一輪他選了不用重加的這一版。
  **要改請先問他。**
- **開場色票刻意不跟淺色模式走**：iOS 的系統啟動圖只吃 manifest 一個固定值。
  代價是**淺色模式下收場時會從深色開場交棒到淺色 App**（實測會看到一次深→淺）。
  這是已知取捨，不是 bug；要不要處理請老闆決定（見下面「還沒拍板」）。
- **`--sp-lead`（340ms）不要拿掉**：iOS 把畫面交出來之前，我們的動畫已經跑掉約 273ms
  （`movie-library` 逐格錄影反推）。那一拍就是在補這段盲窗，`tools/check-splash.js` 會驗
  「白起的四條動畫 delay 全部 ≥ 273ms」。⛔ 不可以改用「把漸深變慢」來掩蓋。
- **關鍵路徑塊畫的是「動畫的起始狀態」不是完成態**（`.sp-name{opacity:0}` ＋
  白起的 `.sp-glyph{opacity:0}`）。畫完成態的話 `splash.css` 一到就會跳回起點 ——
  Benson 在上一支 App 說「有點小奇怪」的就是它。代價：三支 CSS 全 404 時符號與名字不會出現，
  **那是刻意的**（CSS 404 罕見，跳動每次開 App 都發生）。
- **`body{background:transparent}` 那一條（關鍵路徑塊 ＋ `splash.css §7a2` 兩份都要）**：
  `body` 的底色畫在 `html` 畫布**上面**，開場沉下去了 body 那片深色還在。
  只要有一塊畫面沒被 `#splash` 蓋到就會露出來（上一支 App 的錄影拍到過下緣一條 58px 的深色帶）。
  ⚠️ 一定要 `transparent`，不可以讓 body 自己再跑一次同樣的漸變。
- **⛔ `apple-mobile-web-app-status-bar-style` 一定要 `black-translucent`，不可以是 `black`**
  （v21，2026-08-28 修；v20 上線時是 `black`，Benson 當天就回報）。
  症狀：開場漸深時，畫面**最上緣有一條 26 CSS px 的淺色帶跟不上內容**，約 1 秒後才自己跳成深色。
  他的螢幕錄影逐格（1180×2556、59.94fps）：第 160 幀上緣 `#e4e4e4` / 內容 `#4e5054`；
  第 200 幀上緣還是 `#e3e3e3` / 內容已經 `#14161a`；第 260 幀上緣才跳成 `#18191d`。
  根因：`black` ⇒ iOS 在頁面**外面**另外畫一條實心的狀態列底，它取用 `theme-color` 的頻率
  **遠低於每幀** ⇒ §7b 的每幀追蹤器再準也追不上，就卡在最初那片白。
  `black-translucent` ⇒ 狀態列**沒有自己的底**，直接疊在我們畫的畫面上 ⇒ 跟開場一起變，
  **結構上不可能不同步**。`movie-library` 用的就是這一條（Benson 評「完美」），
  而 `js/splash-boot.js §7b` 的註解本來就是照 `black-translucent` 寫的 —— v20 等於違反了模組自己的前提。
  ⚠️ 代價：WebView 延伸到狀態列底下，貼頂／貼底的東西必須自己讓開。
  實測（`tools/probe/safe-area.mjs`，注入 iPhone 15 Pro 的 inset 59/34）：
  `.topbar` padding-top 77px、`#modeBar` y=138.8、`.summary` y=199.8、FAB 距底 56、
  `#kr-full .kr-top` padding-top 69px —— **原本就全部有讓開，一行版面都不用改**。
  ⚠️ 唯一薄的地方：兩個 `.sheet` 的 `max-height:92vh`，內容灌到撞頂時面板頂端落在 y=68.2，
  離 inset 只剩 **9.2px**（393×852 上實測；算式 `0.08×vh − inset`，各機型最薄是 iPhone 16 Pro 的 7.9px）。
  **刻意不動它**：改成 `calc(92vh - var(--safe-t))` 會讓記錄面板的自然高（727px）超過新的
  max-height（724.8px），本來不用捲的面板突然變成要捲 —— 那是看得見的退步。
  真的要調的話請連 `.sheet` 的內容高度一起重新設計。
- **`<meta name="theme-color">` 有兩條（深色／淺色）**，`splash-boot.js §7b` 的追蹤器抓的是
  **第一個** ⇒ **深色那一條一定要排在前面**。淺色模式下追蹤器等於沒作用（改到的是沒生效的那一條），
  無害，但要知道。
  改成 `black-translucent` 之後 §7b **還是要留**（v21 沒動它）：它負責的已經不是狀態列那一帶
  （那塊現在由我們自己畫），而是**頁面之外**剩下的系統色 —— overscroll 回彈區與 home 指示條那一帶
  （`movie-library` 錄影拍到過的下緣 58px 深色帶就是它）。而且 `js/splash-boot.js` 在 SHA 鎖鏈上，
  動它要回範本改、影響所有 App。實測 8 條路徑全過、開場中寫入 100 個相異值、收場換回字面值 `#0F1218`。
- **SHA 鎖鏈**：`css/splash.css`、`js/splash.js`、`js/splash-boot.js` 必須跟
  `../app-template/motion/` 逐位元組相同；`index.html` 柵欄裡那份又必須跟 `js/splash-boot.js` 相同。
  改法是**回範本改**，再 `cp` 過來、再跑
  `node ../app-template/motion/inline-boot.js index.html js/splash-boot.js` 重貼。
  兩段鎖鏈 `tools/check-splash.js` 都會驗（正本不在這台機器時會出聲警告，不會安靜放行）。

### 沒有 npm test，守衛就是這一層的測試

```bash
node tools/check-splash.js index.html manifest.webmanifest          # 約 25 秒（含全色域窮舉）
node tools/check-splash.js index.html manifest.webmanifest --quick  # 跳過窮舉，約 3 秒
node tools/probe/first-frame.mjs   # 第一幀 ＝ 動畫起始狀態（自帶負控組）
node tools/probe/press-scan.mjs    # 按下回饋全掃描（真滑鼠事件，自帶負控組）
node tools/probe/paths.mjs         # 八條路徑：冷啟動／CSS 遲到／CSS 404／splash.js 404／JS 停用／熱啟動／淺色／reduce
node tools/probe/flows.mjs         # 面板開關 ＋ 剛存那一筆的高亮
node tools/probe/status-band.mjs [dark|light|reduce|hot]   # 頂端不准有硬邊（v21 那個 bug 的常駐防線）
node tools/probe/safe-area.mjs   [dark|light]              # 注入真 inset，逐項量貼頂/貼底元素有沒有讓開
```

- `tools/check-splash.js` 是範本 `check-splash.js` 的複製品，**只打了四個落地補丁**
  （模組檔改到 `css/`、`js/` 找；路徑判準不寫死 `motion/`；裸呼叫掃描認檔名不認資料夾；
  色票那一節換成「motion.css 零顏色 ＋ 色票仍住在 style.css」並**明講對比度不在涵蓋範圍內**）。
  範本改版時重新複製再把那四段貼回去。
- **探針一律先把 `'serviceWorker' in navigator` 換掉再量**：這支 App 的 `index.html` 在
  `controllerchange` 時會 `location.reload()`，第一次進站 SW 一 claim 就整頁重載，
  量測會被沖掉而且第二次載入是「熱啟動」（實測 `navType="reload"`、`cold=false`，
  第一版探針因此拿到 0 個樣本）。⚠️ `Network.setBlockedURLs` **擋不住** SW 的 script 抓取。
- **量 App 底色不可以取畫面左上角**：`body` 有一層
  `radial-gradient(1200px 500px at 50% -8%, rgba(227,169,81,.06), transparent 70%)`，
  上半部整片被染掉約 3%（深色 `#0f1218` → `#161619`）；最下緣又被 FAB 的
  `box-shadow` 染暗一階（淺色 `#f3f5f8` → `#f2f4f7`）。
  正確取樣點是**左邊界、畫面 62%／74% 兩個高度**，而且兩點必須一致（尺的自證）。
  第一版就是取角落，八條路裡有四條被誤判成紅燈。

### ⚠️ 已知：SW 的自動 reload 會打斷開場

`index.html` 的 `controllerchange → location.reload()` 是這個專案自己的決定
（「新版接手後自動重載一次，不用手動清快取」）。**每次部署新版之後的第一次開啟**
會在開場播到一半時整頁重載；重載後是同一個 session ⇒ 熱啟動 ⇒ 開場不重播，
所以看起來是「開場演到一半突然變成 App」。上一支 App（`movie-library`）在同一個問題上
選的是「只跳更新提示、不自動 reload」。**這一輪沒有動它**（不在工單範圍），要不要改請 PM 決定。

## PWA 注意事項

- 改前端要 **`APP_VER` 與 `sw.js` 的 CACHE 版本一起 +1**。
  新增前端檔案時 `sw.js` 的 `SHELL` 也要跟著加（`index.html` 引用到的每一支本站 css/js 都要在裡面）。
  ⚠️ `js/splash-boot.js` **刻意不在 SHELL 裡**：它已經 inline 進 `index.html`，沒有人會 fetch 它。
- SW 註冊要帶 `updateViaCache:'none'`，fetch 對 HTML/JS/CSS 用 `cache:'no-store'`，
  否則瀏覽器那層快取會讓手機**永遠停在舊版**（踩過）。
- 「匯入備份」是**合併**不是覆蓋（原本是覆蓋，同步一次就會蓋掉手機上的紀錄）。
- `pullPractice()` 對已經拉過的那一天**只補空白的 note、不覆蓋**：
  心得常常是交易同步過來之後才在面板上補寫的，不補就永遠傳不到手機；
  但覆蓋會蓋掉他直接在手機上打的字。
- 只同步練習（sim）紀錄到 GitHub；**真實交易不上傳**（repo 是公開的，Benson 的決定）。
  這條在雙向同步之後更重要：手機推上去的 `data/phone.json` 也**只放 sim**，
  真實交易的心得永遠只留在手機上。

## 雙向同步（2026-08-25）

以前只有「面板 → 手機」。手機那邊寫的心得傳不回來，面板看到的還是 8/11 手動匯出的
`my_trades.json`（停在 7/24）。現在兩個方向各走各的檔案，不會互相蓋掉：

| 方向 | 誰寫 | 檔案 | 誰讀 |
|---|---|---|---|
| 面板 → 手機 | `sync_to_cloud()`（git push） | `data/practice.json` | `pullPractice()` |
| 手機 → 面板 | 手機用鑰匙圈的金鑰打 GitHub Contents API | `data/phone.json` | `poll_phone()` 每 3 分鐘 |

- **心得誰新誰贏**，靠 `note_at` 時間戳。`noteWins()`（js）與 `_note_wins()`（py）
  **必須是同一套規則**，不一致就會兩邊來回互蓋。改一邊一定要改另一邊。
- **改心得（含清空）一律換時間戳**；只改進出場價不換。不然編輯價格會讓那筆
  「看起來比較新」，把對面比較新的心得蓋掉；而清空不換戳的話，刪掉的字會被補回來。
- `pull_from_phone()` **只碰 `note` / `note_at`**，不新增／刪除／改動任何一筆交易 ——
  練習紀錄的真相在這台電腦（只有面板知道成交價與 `_mfe` 那些欄位）。
- **手機也會往同一個 repo 寫**，所以 `sync_to_cloud()` 的 push 失敗要
  `git pull --rebase --autostash` 再推一次，否則手機推過之後面板就永遠推不上去。
- 手機端**內容沒變就不送 PUT**，不然每開一次 App 就洗一版 commit。
- **配對不可以把 `time` 算進去**（2026-08-25 踩到，第一版就是這樣寫的）。
  手機的表單以前是「整筆重建」，編輯一筆就會把 `time` 弄丟 ——
  而「在手機上編輯過」跟「有心得」剛好是同一批紀錄，所以真正要同步的那幾筆
  **100% 對不上**，看起來就像功能整個沒作用。現在配對是「日期＋進場價」，
  進場價也被改過而那天兩邊都只有一筆時退回用日期認。手機端也已改成沿用舊欄位。
- 鑰匙圈：`appId` 是 `trade-log`、`tokenKey` 是 `tradelog_gh_pat`，模組在 `js/keyring-unlock.js`
  （keyring repo 的 `sync-unlock.yml` 會自動更新它；trade-log 的目錄是 `js`，不是 `public`/`docs`）。
  **金鑰要 Benson 自己在鑰匙圈後台配**（fine-grained PAT，只授權 xd1104/trade-log，
  Contents: Read and write）。開發一律用假 token。

## 排程

`trade-log-daily-append`（工作日 14:10）→ 跑 `append_today.py`：
抓當天 1 分 K → 併進 `tmf_1min.csv` → 重算 `intraday.csv` → 印前後對照。
每天只多 1 天樣本，數字幾乎不會動 —— **沒變化就老實說沒變化**，不要把雜訊講成趨勢。

- **每次都要回頭補抓前 4 天**：14:10 跑的時候當晚夜盤還沒開始，只抓「今天」的話
  15:00~23:59 那段永遠補不回來，隔天再抓隔天也補不到。週六同理（週五夜盤延到週六凌晨，
  但週六排程不跑）。重複的 ts 由 `drop_duplicates` 濾掉。
- **排程會被重裝清掉**：`~/.claude/scheduled-tasks/` 底下資料夾還在、但註冊被清空，
  看起來像有在跑其實沒有。2026-08-21 就是這樣讓 `tmf_1min.csv` 停在 08-11 十天沒人發現。
  懷疑資料沒更新時，先比對 csv 的最後一天跟今天差幾天。
