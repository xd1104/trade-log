# Python
.venv/
__pycache__/
*.pyc

# 機密：API Key / Secret 只留本機
.env
tools/shioaji/.env

# 匯出的行情資料（檔案很大，不進版控）
*.csv

# shioaji 執行時產生的 log
shioaji.log

panel.log

# 你的交易紀錄匯出檔（個人資料，不進版控）
tools/shioaji/my_trades.json
tools/shioaji/practice_trades/

# 【回顧】分頁產生的本機檔案（個人資料，repo 是公開的）
tools/shioaji/replay_log/
tools/shioaji/review_cache.json

# 桌面 App 的執行記錄（本機）
tools/shioaji/panel-app.log

# 真實下單：紀錄與開關都只留本機（開關檔存在＝會真的送單）
tools/shioaji/real_orders/
tools/shioaji/sim_orders/
tools/shioaji/REAL_ORDERS_ON

# 憑證：絕對不進版控
*.pfx
*.p12
tools/shioaji/real_trades/

# 逐筆報價紀錄（很大，而且是市場資料，不進版控）
tools/shioaji/tick_logs/

# 【自動下單（模擬）】分頁的模擬紀錄（含進場價與時間，repo 是公開的）
tools/shioaji/autotest/

# 【策略實驗室】的歷史逐筆（市場資料、很大）＋每日摘要與快取。
# ⚠️ 上面那條 *.csv 擋不到 .csv.gz / .npz / .jsonl，一定要整個資料夾擋。
tools/shioaji/tick_hist/

# 【自動下單】：開關與紀錄都只留本機（開關檔存在＝程式會自己送單）
# ⚠️ 用 * 是因為畫面上那顆「關閉」鈕是**改名**（AUTO_ORDERS_ON.off-YYYYmmdd-HHMMSS）
#    不是刪掉 —— 內容留著他才好再開回去，而那個檔一樣不可以進版控。
tools/shioaji/AUTO_ORDERS_ON*
tools/shioaji/autofire/
# 【自動下單】「開盤快才做」的開盤走幅歷史（每天 09:00 以前最後一筆／09:03:30 的價，2026-09-15 加）。
# ⚠️ 上面那條 *.csv 擋不到 .jsonl；`*.jsonl.*` 擋種子腳本測試時的暫存檔名。
tools/shioaji/fast_hist.jsonl
tools/shioaji/fast_hist.jsonl.*
# 【自動下單】「開箱」的箱子寬度歷史（每天 09:00~09:05 的最高／最低，2026-09-16 加）。
tools/shioaji/orb_hist.jsonl
tools/shioaji/orb_hist.jsonl.*
# ⛔⛔ 【帳戶總覽】每天一列的權益數字（2026-09-21 加）——**真實金額，絕對不上傳**
#    （跟 real_orders/、real_trades/ 同一條鐵律：repo 是公開的）。
tools/shioaji/equity/

# 【策略實驗室】「模擬（不會下單）」的每日模擬結果（含進出場價，repo 是公開的；2026-09-15 晚上加）
# ⚠️ 上面那條 *.csv 擋不到 .jsonl，一定要整個資料夾擋。
tools/shioaji/sim_lanes/
